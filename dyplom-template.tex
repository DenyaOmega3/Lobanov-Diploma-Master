\documentclass{thesis}

\renewcommand{\baselinestretch}{1.20}


\begin{document}
\allowdisplaybreaks

\large

\setcounter{page}{1}
\thispagestyle{empty}
\centerline{Національна академія наук України}
\centerline{Міністерство освіти і науки України}
\centerline{Державна наукова установа «Київський академічний університет»}

\vspace{10mm}

\begin{flushright}
\begin{minipage}{100mm}
\begin{center}\large {\bf <<Допущено до захисту>>}\\
Завідувач кафедри математики,\\
доктор фіз.-мат. наук\\
{\bf Вячеслав БОЙКО}\\
<<\underline{\hspace{8mm}}>> червня 2025 р.
\end{center}
\end{minipage}
\end{flushright}

\vspace{10mm}

\centerline{\Large \bf Лобанов Денис Артемович}

\begin{center}
{\bf КВАЛІФІКАЦІЙНА РОБОТА}\\
на здобуття освітнього ступеня <<магістр>>\\
Спеціальність 111 <<Математика>>\\[4mm]
{\Large \bf Тема: <<Опис кубічних кілець зі скінченним числом незвідних зображень>>}
\end{center}


\vspace{5mm}

\noindent
{Засвідчую, що кваліфікаційна робота містить результати власних досліджень. Використання ідей, результатів і~текстів інших авторів мають посилання на відповідне джерело.
\underline{\hspace{18mm}} Д.А.~ЛОБАНОВ\par}

\vspace{5mm}

\begin{flushright}
\begin{minipage}{90mm}
\large {\bf Науковий керівник}\\
доктор фіз.-мат. наук, професор\\
{\bf Дрозд Юрій Анатолійович}\\
\underline{\hspace{48mm}}

\end{minipage}
\end{flushright}


\vfill

\centerline{\bf Київ --- 2025}

\newpage

\begin{center}
\Large \bf Анотація
\end{center}

\noindent
\textbf{Лобанов Д.А.}, \textbf{Опис кубічних кілець зі скінченним числом незвідних зображень}, Кваліфікаційна робота на здобуття освітнього ступеня <<магістр>> за спеціальністю 111 Математика, Київський академічний університет, Київ, 2025, ??~с., ??~джерел.

\bigskip

Метою даної магістерської роботи є обширне дослідження розширень кубічних кілець із теоретичної так і з алгоритмічної точки зору, а також спроба описати ті кубічні кільця, які мають скінченну кількість неізоморфних нерозкладених зображень.

Упродовж даного періоду часу була побудована та розв'язна система рівнянь, яка включає можливість подальшого розширення кубічного кільця. Головними результатами є певна (узагальнена) класифікація кілець із властивістю, що були згадані вище.

У якості допоміжного інструменту була використана мова програмування Python. Для перевірки достовірності були створені клас, описуючи кубічне кільце, та клас, описуючи ланцюг розширень. Крім того, був створений алгоритм, який дозволяє розширення та базиси для кожного кільця.

Подальшим планом буде продовжити відокремлювати деякі класи кубічних кілець, які задовольняють бажану умову.

\bigskip

\noindent
{\bf MSC:} 11A07, 13A99, 11R33.

\bigskip

\noindent
{\bf Ключові слова:} кубічне кільце, система конгруентних рівнянь, символ Лежандра, цілочисельне зображення, відповідність Делоне-Фадєєва.
\bigskip

\newpage

\begin{center}
\Large \bf Abstract
\end{center}

\noindent
\textbf{Lobanov D.A.}, \textbf{Cubic ring description with finite number of irreducible representations}, Master Thesis, speciality 111 Mathematics.~--
Kyiv Academic University, Kyiv, 2025, ??~pages, ??~references.

\bigskip
The aim of the Master thesis is a wide research of cubic ring extensions from theoretical and algorithmic perspective, and also an attempt to describe such cubic rings that have finitely many nonisomorphic indecomposable representations.

During this period of time a system of equations was constructed solved which involves a possibility to a further extension of a cubic ring. The main results are some (general) classification of rings with properties mentioned above.

The programming language Python has been used as an auxiliary tool. For checking for a reliability two classes were created: a class describing a cubic ring and a class describing an extension chain. Moreover, the algorithm producing an extension with bases for each ring was scripted.

The further plan will be to continue to isolate some classes of cubic rings that satisfy the desired condition.

\bigskip

\noindent
{\bf MCS:} 11A07, 13A99, 11R33. %for 111, see https://mathscinet.ams.org/mathscinet/msc/msc2020.html

%\noindent
%{\bf ACM:} ??????? %for 122, see https://cran.r-project.org/web/classifications/ACM.html

\bigskip

\noindent
{\bf Key words:} cubic ring, system of congruent equations, Legendre symbol, integral representation, Delone-Faddeev correspondence.

\newpage

\tableofcontents

\newpage

\phantomsection
\section*{Перелік умовних позначень}
\addcontentsline{toc}{chapter}{Перелік умовних позначень}

\bigskip


\begin{tabular}{ll}
$\mathbb{Z}$ & кільце цілих чисел\\
$\Lambda$ & кубічне кільце\\
$f$ & кубічна форма (індекс-форма) кільця\\
$\Delta$ & дискримінант кубічної форми\\
$\Delta_\Lambda$ & дискримінант кубічного кільця $\Lambda$\\
$[\theta]$ & матриця оператора множення на елемент $\theta$\\
$M_{2 \times 2}$ & сума всіх діагональних мінорів $2 \times 2$\\
$\left( \dfrac{\cdot}{p} \right)$ & символ Лежандра
\end{tabular}


\newpage

\phantomsection
\chapter*{Вступ}\label{Introduction}
\addcontentsline{toc}{chapter}{Вступ}

??????????
\cite{boyko-thesis,boyko2021,PopovychBoykoNesterenkoLutfullin2003}

\newpage

\chapter{Максимальне кубічне кільце}\label{chaper1}

\section{Попередні відомості про кубічні кільця}\label{section1.1}
Необхідно пригадати важливі означення та результати.
\begin{definition}
\textbf{Кубічним кільцем} $\Lambda$ називають вільний $\mathbb{Z}$-модулем рангу $3$.
\end{definition}

Оскільки кубічне кільце містить елемент $1$, то буде зручніше надалі обирати базис вигляду $\{1,\omega_1,\omega_2\}$.

\begin{definition}
Базис $\{1,\omega_1,\omega_2\}$ кубічного кільця $\Lambda$ називається \textbf{нормальним}, якщо
\begin{center}
$\omega_1 \cdot \omega_2 \in \mathbb{Z}$.
\end{center}
\end{definition}

\begin{remark}
Зафіксуємо базис $\{1,\omega_1,\omega_2\}$ кільця $\Lambda$. Тоді $\omega_1 \cdot \omega_2 = k \omega_1 + l \omega_2 + m$ при $k,l,m \in \mathbb{Z}$. Замінивши $\omega_1$ на $\omega_1-l$ та $\omega_2$ на $\omega_2 - k$, отримуємо нормальний базис $\{1,\omega_1 - l, \omega_2 - k\}$.
\end{remark}

Отже, \textcolor{red}{надалі всі базиси в кубічних кільцях -- нормальні}.

Запишемо таблицю множення для базиса $\{1,\omega_1,\omega_2\}$:
\begin{align*}
\omega_1^2 & = b \omega_1 + a \omega_2 + r \\
\omega_1 \omega_2 & = s \\
\omega_2^2 & = d \omega_1 + c \omega_2 + t
\end{align*}
Прирівнявши $(\omega_1^2) \cdot \omega_2 = \omega_1 \cdot (\omega_1 \cdot \omega_2)$ та $\omega_1 \cdot (\omega_2^2) = (\omega_1 \cdot \omega_2) \cdot \omega$, отримуємо співвідношення $r = -ac,\ s = ad,\ t = -bd$. Отже, таблиця множення задається чотирма числами $a,b,c,z \in \mathbb{Z}$:
\begin{align*}
\omega_1^2 & = b \omega_1 + a \omega_2 - ac \\
\omega_1 \omega_2 & = ad \\
\omega_2^2 & = d \omega_1 + c \omega_2 + -bd
\end{align*}

\begin{definition}
Нехай $\Lambda$ -- кубічне кільце та $M \subset \Lambda$ -- решітка, тобто $M = \left<\theta_1,\theta_2,\theta_3\right>$. Розкладемо кожний $\theta_i$ за базисом $\{1,\omega_1,\omega_2\}$ таким чином:
\begin{align*}
\theta_1 = c_{10} \cdot 1 + c_{11} \cdot \omega_1 + c_{12} \omega_2 \\
\theta_2 = c_{20} \cdot 1 + c_{21} \cdot \omega_1 + c_{22} \omega_2 \\
\theta_3 = c_{30} \cdot 1 + c_{31} \cdot \omega_1 + c_{32} \omega_2
\end{align*}
\textbf{Індексом решітки $M$} називатимемо значення:
\begin{align*}
[\Lambda : M] = \left| \det \begin{pmatrix}
c_{10} & c_{20} & c_{30} \\
c_{11} & c_{21} & c_{31} \\
c_{12} & c_{22} & c_{32} \\
\end{pmatrix} \right|.
\end{align*}
\end{definition}

Розглянемо кубічне кільце $\Lambda = \left<1,\omega_1,\omega_2\right>$, а також решітку $\mathbb{Z}(\alpha) = \left<1,\alpha,\alpha^2\right>$, де елемент $\alpha = z + x \omega_1 + y \omega_2$. Знайдемо індекс $[\Lambda : \mathbb{Z}(\alpha)]$. Детально розпишемо елемент $\alpha^2$.
\begin{align*}
\alpha^2 & = (z + x \omega_1 + y \omega_2)^2 = z^2 + x^2 \omega_1^2 + y^2 \omega_2^2 + 2zx \omega_1 + 2zy \omega_2 + 2 xy \omega_1 \omega_2 \\
& = z^2 + x^2 (b \omega_1 + a \omega_2 - ac) +y^2 (d \omega_1 + c \omega_2 - bd) \\ 
& + 2zx \omega_1 + 2yz \omega_2 + 2xyad
\end{align*} 
Отже, маємо ось такий розклад:
\begin{multline*}
1 = 1 \cdot 1 + 0 \cdot \omega_1 + 0 \cdot \omega_2 \\
\alpha = z \cdot 1 + x \omega_1 + y \omega_2 \\
\alpha^2 = (z^2 - x^2 ac - y^2 bd + 2xyad) \cdot 1 + (x^2 b + y^2 d + 2xz) \omega_1 + (x^2 a + y^2 c + 2yz) \omega_2
\end{multline*}
Отже, індекс обчислюється таким чином:
\begin{align*}
[\Lambda : \mathbb{Z}(\alpha)] & = \left| \det \begin{pmatrix}
1 & z & z^2-x^2ac-y^2bd+2xyad \\
0 & x & x^2b+y^2d+2xz \\
0 & y & x^2a+y^2c+2yz
\end{pmatrix} \right| \\
& = |x(x^2a+y^2c+2yz) -y(x^2b+y^2d+2xz)| \\
& = |ax^3 + cxy^2 - bx^2y - dy^3|.
\end{align*}

\begin{definition}
\textbf{Кубічною формою} (або \textbf{індекформою}) називатимемо вираз
\begin{align*}
f(x,y) = ax^2 + cxy^2 - bx^2y - dy^2
\end{align*}
Його дискримінант обчислюється за формулою:
\begin{align*}
\Delta = b^2 c^2 - 4c^3 a - 4b^3 d + 18 abcd - 27a^2d^2.
\end{align*}
\end{definition}

У результаті отримали, що кожному кубічному кільцю $\Lambda$ відповідатиме з точністю до знаку кубічна форма $f(x,y)$.

І навпаки, будь-якою кубічною формою $f(x,y)$, що задається числами $a,b,c,d \in \mathbb{Z}$ з ненулевим дискримінантом, можна визначити кубічне кільце $\Lambda$.

Нехай $\Lambda$ -- кубічне кільце та $\alpha \in R$. Розглянемо оператор множення на елемент $\alpha$, тобто $x \mapsto \alpha x$. Якщо $\{\alpha_1,\alpha_2,\alpha_3\}$ -- базис кільця $\Lambda$ (довільний базис), то оператору відповідатиме матриця $[\alpha]$, від якої можна знайти слід $\tr \alpha$.
 
\begin{definition}
\textbf{Дискримінантом кубічного кільця}  $\Lambda$ назвемо матрицю:
\begin{align*}
\Delta_\Lambda = \det \begin{pmatrix}
\tr(\alpha_1 \alpha_1) & \tr(\alpha_1 \alpha_2) & \tr(\alpha_1 \alpha_3) & \\
\tr(\alpha_2 \alpha_1) & \tr(\alpha_2 \alpha_2) & \tr(\alpha_2 \alpha_3) & \\
\tr(\alpha_3 \alpha_1) & \tr(\alpha_3 \alpha_2) & \tr(\alpha_3 \alpha_3) & \\
\end{pmatrix}
\end{align*}
\end{definition}

\begin{remark}
Дискримінант кільця $\Delta_\Lambda$ не залежить від вибору базису. Тому надалі можемо працювати з нормальним базисом $\{1,\omega_1,\omega_2\}$. Маючи даний базис та таблицю множення, отримуємо
\begin{align*}
\Delta_\Lambda & = \det \begin{pmatrix}
\tr 1 & \tr \omega_1 & \tr \omega_2 \\
\tr \omega_1 & \tr \omega_1^2 & \tr \omega_1 \omega_2 \\
\tr \omega_2 & \tr \omega_1 \omega_2 & \tr \omega_2^2
\end{pmatrix} = \det \begin{pmatrix}
3 & b & c \\
b & b^2-2ac & 3ad \\
c & 3ad & c^2-2bd
\end{pmatrix} \\
& = b^2 c^2 - 4c^3 a - 4b^3 d + 18abcd - 27a^2d^2 = \Delta.
\end{align*}
Отже, дискримінант кубічного кільця $\Lambda$ дорівнює дискримінанту кубічної форми $f(x,y)$.
\end{remark}

\iffalse
\begin{definition}
\textbf{Ступінчастий базис відносно $\omega$} кубічного кільця $\Lambda$ має вигляд:
\begin{align*}
\left\{ 1,\ \dfrac{u\omega + v}{\Delta},\ \dfrac{\lambda \omega^2 + \mu \omega + \nu}{\Delta} \right\},
\end{align*}
де $u,v,\lambda,\mu,\nu,\Delta \in \mathbb{Z}$.
\end{definition}

\begin{theorem}[Вороного]
\label{voronogo}
Нехай $\rho$ -- кубічне ціле число, що задовольняє незвідне рівняння $F(\rho) = \rho^3 - s \rho^2 - q \rho - n = 0$. Нехай $\delta,a,\Delta_{\Lambda},t \in \mathbb{Z}$ задовольняють такі умови:
\begin{align*}
\Delta = \delta^6 a^2 \Delta_\Lambda, \\
\dfrac{1}{2} F''(t) \equiv 0 \pmod {\delta}, \quad F'(t) \equiv 0 \pmod {\delta^2 a}, \quad F(t) \equiv 0 \pmod{\delta^3 a^2}
\end{align*}
Число $\Delta$ -- дискримінант рівняння $F(\rho) = 0$. Тоді система $\left\{ 1,\ \dfrac{\rho - t}{\delta},\ \dfrac{\rho^2 + (t-s)\rho + (t^2 - st + q)}{\delta^2 a} \right\}$ -- нормальний та ступінчастий базис відносно $\rho$.
\end{theorem}
\fi

\section{Умови розширення кільця}
\begin{definition}
Кубічне кільце $\Lambda$ будемо називати \textbf{немаксимальним}, якщо існує більше кубічне кільце $\tilde{\Lambda} \supsetneq \Lambda$. Іншими словами, існує елемент $\theta \in \tilde{\Lambda}$, при цьому $\theta \notin \Lambda$.
\end{definition}

Якщо кубічне кільце $\Lambda$ є немаксимальним, то, взявши елемент $\theta \notin \Lambda$, можемо розглянути розширення $\tilde{\Lambda} = \Lambda[\theta]$. З'ясуємо, за яких умов $\tilde{\Lambda}$ стане кубічним кільцем також.

\subsection{$\theta^3 \in \tilde{\Lambda}$}
Елемент $\rho \in \tilde{\Lambda}$ записується як многочлен із $\mathbb{Z}[\theta]$.  Оскільки нас цікавить модуль рангу $3$, один із варіантів -- це вимагати, щоб $\theta^3 \in \tilde{\Lambda}$. У такому разі кожний елемент записується в такому вигляді:
\begin{align*}
\rho = x + y \theta + z \theta^2.
\end{align*}
Зрозуміло, що $\{ 1,\theta,\theta^2 \}$ утворює базис кільця $\tilde{\Lambda}$. Коли $\theta^3 \in \tilde{\Lambda}$, даний базис задаватиме кубічне кільце $\tilde{\Lambda}$.

Оскільки $\omega_1,\omega_2 \in \Lambda$, то звідси $\omega_1,\omega_2 \in \tilde{\Lambda}$ та записуються в лінійну комбінацію $1,\theta,\theta^2$.  У такому разі елемент $\theta$ зобов'язаний мати вигляд:
\begin{align*}
\theta = \dfrac{\alpha + \beta \omega_1 + \gamma \omega_2}{p}
\end{align*}
при $\alpha,\beta,\gamma \in \mathbb{Z}$ та також $p \in \mathbb{Z}$. 

\begin{remark}
Вибір $\alpha,\beta,\gamma$ може бути обмеженим до $\pmod p$.
\end{remark}

Справді, нехай $\Lambda = \left< 1,\omega_1,\omega_2\right>$ та припустимо ми маємо два розширення $\tilde{\Lambda}_1 = \left< 1, \theta, \theta^2\right>$ та $\tilde{\Lambda}_2 = \left< 1, \sigma, \sigma^2\right>$, де елементи $\theta = \dfrac{\beta \omega_1 + \gamma \omega_2 + \alpha}{p}$ та $\sigma = \dfrac{\beta' \omega_1 + \gamma' \omega_2 + \alpha'}{p}$, причому $\alpha' \equiv \alpha,\ \beta' \equiv \beta,\ \gamma' \equiv \gamma \pmod p$. Іншими словами, $\alpha' = \alpha + px,\ \beta' = \beta + py,\ \gamma' = \gamma + pz$ при деяких $x,y,z \in \mathbb{Z}$. Розпишемо $\sigma$ на основі цих рівностей:
\begin{align*}
\sigma & = \dfrac{\beta \omega_1 + py \omega_1 + \gamma \omega_2 + pz \omega_2 + \alpha + px}{p} \\
& = \dfrac{\beta \omega_1 + \gamma \omega_2 + \alpha}{p} + y \omega_1 + z \omega_2 + x = \theta + x + y \omega_1 + z \omega_2.
\end{align*}
Оскільки $1,\omega_1,\omega_2 \in \tilde{\Lambda}_1$ та $\theta \in \tilde{\Lambda}_1$, то звідси $\sigma \in \tilde{\Lambda}_1$. Водночас через те, що $1,\omega_1,\omega_2 \in \tilde{\Lambda}_2$ та $\sigma \in \tilde{\Lambda}_2$, то, виразивши $\theta$, отримуємо $\theta \in \tilde{\Lambda}_2$.

Отже, отримуємо $\tilde{\Lambda}_1 = \tilde{\Lambda}_2$.

\textcolor{red}{Надалі обмежуємось випадком, коли $\gamma = 0$}.

Час відповісти на питання, коли $\theta^3 \in \tilde{\Lambda}$. Для цього розглянемо оператор множення на елемент $\theta$, тобто $\lambda \mapsto \theta \cdot \lambda$. З'ясуємо, як буде виглядати матриця $[\theta]$ даного оператора.
\begin{align*}
\theta \cdot 1 = \dfrac{\alpha + \beta \omega_1}{p} = \dfrac{\alpha}{p} \cdot 1 + \dfrac{\beta}{p} \omega_1 + 0 \cdot \omega_2
\end{align*}
\begin{align*}
\theta \cdot \omega_1 & = \dfrac{\alpha + \beta \omega_1}{p} \cdot \omega_1 = \dfrac{\alpha \omega_1}{p} + \dfrac{\beta (-ac + b \omega_1 + a \omega_2)}{p} = \\
& = \dfrac{-\beta a c}{p} \cdot 1 + \dfrac{\alpha + \beta b}{p} \cdot \omega_1 + \dfrac{\beta a}{p} \cdot \omega_2
\end{align*}
\begin{align*}
\theta \cdot \omega_2 = \dfrac{\alpha + \beta \omega_1}{p} \cdot \omega_2 = \dfrac{\alpha \omega_2 + \beta ad}{p} = \dfrac{\beta a d}{p} \cdot 1 + 0 \cdot \omega_1 + \dfrac{\alpha}{p} \cdot \omega_2
\end{align*}

Зібравши всі коефіцієнти, отримуємо такий вигляд матриці:
\begin{align*}
[\theta] = \dfrac{1}{p} \begin{pmatrix}
\alpha & -\beta a c & \beta a d \\
\beta & \alpha + \beta b & 0 \\
0 & \beta a & \alpha
\end{pmatrix}
\end{align*}

Запишемо характеристичний многочлен даної матриці:
\begin{align*}
\chi_{[\theta]}(t) = \det [\theta] - M_{2 \times 2}[\theta] \cdot t + \tr [\theta] \cdot t^2 - t^3,
\end{align*}
де
\begin{align*}
M_{2 \times 2}[\theta] = M_{12}^{12}[\theta] + M_{13}^{13}[\theta] + M_{23}^{23}[\theta].
\end{align*}
Іншими словами, $M_{2 \times 2}[\theta]$ -- сума всіх діагональних мінорів $2 \times 2$.

Отже, $\chi_{[\theta]}(\theta) = 0$. Це означає, $\theta^3 = \det [\theta] - M_{2 \times 2}[\theta] \cdot \theta + \tr [\theta] \cdot \theta^2$. Оскільки нам необхідно $\theta^3 \in \left<1,\theta,\theta^2\right>$, треба вимагати, щоб
\begin{align*}
\det [\theta],\ M_{2 \times 2}[\theta],\ \tr [\theta] \in \mathbb{Z}.
\end{align*}

Обчислимо кожний коефіцієнт.
\begin{align*}
\tr [\theta] = \dfrac{1}{p} (3 \alpha + \beta b) \\
\det [\theta] = \dfrac{1}{p^3} \left( \alpha^3 + \alpha^2 \beta b + \beta^3 a^2 d + \alpha \beta^2 ac \right)
\end{align*}
Детальніше розпишемо знаходження $M_{2 \times 2}[\theta]$. Маємо
\begin{align*}
M_{2 \times 2}[\theta] & = M_{12}^{12}[\theta] + M_{13}^{13}[\theta] + M_{23}^{23}[\theta] = \\
& = \dfrac{1}{p^2} \det \begin{pmatrix}
\alpha & -\beta \alpha c \\
\beta & \alpha + \beta b
\end{pmatrix} + \dfrac{1}{p^2} \det \begin{pmatrix}
\alpha & \beta a \alpha \\
0 & \alpha
\end{pmatrix} + \dfrac{1}{p^2} \det \begin{pmatrix}
\alpha + \beta b & 0 \\
\beta a & \alpha
\end{pmatrix} = \\
& = \dfrac{1}{p^2} \left( \alpha^2 + \alpha \beta b + \beta^2 \alpha c + \alpha^2 + \alpha^2 + \alpha \beta b \right) = \\ 
& = \dfrac{1}{p^2} \left( 3 \alpha^2 + \beta^2 a c + 2 \alpha \beta b\right)
\end{align*}

Із урахуванням необіхдних умов на ці числа, запишемо цю умову в термінах конгруентних рівнянь:
\begin{equation}
    \begin{cases}
      \alpha^3 + \alpha^2 \beta b + \beta^3 a^2 d + \alpha \beta^2 a c & \equiv 0 \pmod {p^3} \\
    3\alpha^2 + \beta^2 ac + 2 \alpha \beta b & \equiv 0 \pmod {p^2} \\
      3 \alpha + \beta b & \equiv 0 \pmod p 
    \end{cases}\,.
\end{equation}
\textcolor{red}{Надалі припускаємо, що $p$ -- просте число та $\alpha \neq 0$.} Тоді можна підібрати елемент $\alpha^{-1}$ -- оборотний елемент класу лишків $\mathbb{Z}_p$ відносно множення. Третє, друге, перше рівняння домножуємо на відповідно $\alpha^{-1},\ \alpha^{-2},\ \alpha^{-3}$ -- отримуємо наступне:
\begin{equation*}
    \begin{cases}
      1 + (\beta \alpha^{-1}) b + (\beta \alpha^{-1})^{3} a^2 d + (\beta \alpha^{-1})^2 a c & \equiv 0 \pmod {p^3} \\
    3 + ((\beta \alpha^{-1})^2 ac + 2 (\beta \alpha^{-1}) b & \equiv 0 \pmod {p^2} \\
      3 + (\beta \alpha^{-1})b & \equiv 0 \pmod p 
    \end{cases}\,.
\end{equation*}
Проведемо заміну:
\begin{align}
t = \beta \alpha^{-1}
\end{align}
Задача звелась до розв'язання системи конгруентних рівнянь відносно $t$, якщо $p$ -- просте число: 
\begin{equation}
\label{system_congruent_equations}
    \begin{cases}
      a^2 d t^3 + ac t^2 + bt + 1 & \equiv 0 \pmod {p^3} \\
      act^2 + 2bt + 3 & \equiv 0 \pmod {p^2} \\
      3 + bt & \equiv 0 \pmod p 
    \end{cases}\,.
\end{equation}

\iffalse
Підіб'ємо підсумок:
\begin{proposition}
Нехай $\Lambda$ -- кубічне кільце, яке задається числами $(a,b,c,d)$. Тоді $\Lambda$ -- немаксимальне $\iff$ існує розв'язок системи рівнянь \eqref{system_congruent_equations}.
\end{proposition}
\fi
На цьому ще не все. Розпишемо елемент $\omega_1$ через лінійну комбінацію $\{1,\theta,\theta^2\}$. Маємо наступне:
\begin{align*}
\omega_1 = \xi \cdot 1 + \eta \cdot \dfrac{\alpha + \beta \omega_1}{p} + \zeta \cdot \dfrac{\alpha^2 + 2 \alpha \beta \omega_1 + \beta^2 (b \omega_1 + a \omega_2 - ac)}{p^2}
\end{align*}
Прирівнюючи коефіцієнти, отримуємо наступне:
\begin{align*}
\xi + \eta \cdot \dfrac{\alpha}{p} + \zeta \cdot \dfrac{\alpha^2 - \beta^2 ac}{p^2} = 0 \\
\eta \cdot \dfrac{\beta}{p} + \zeta \cdot \dfrac{2\alpha\beta + \beta^2 b}{p^2} = 1 \\
\zeta \cdot \dfrac{\beta^2 a}{p^2} = 0
\end{align*}
Отже, звідси $\zeta = 0$, а також $\eta = \dfrac{p}{\beta}$. Проте оскільки $p$ -- просте число за припущенням, то $\beta \in \{1,p\}$. Далі, маємо $\xi = - \dfrac{\alpha}{\beta}$.

Якщо $\beta = 1$, то звідси $\xi = -\alpha$, отримуємо цілий коефіцієнт.

Якщо $\beta = p$, то звідси $\xi = -\dfrac{\alpha}{p}$. Отже, вимагаємо $\alpha \equiv 0 \pmod p$. Проте в цьому випадку $\theta = \dfrac{\alpha + \beta \omega_1}{p} = \dfrac{\alpha}{p} + \omega_1 \in \Lambda$. Тобто буде $\Lambda = \tilde{\Lambda}$, не виникне розширення.

\begin{lemma}
Нехай $\Lambda$ -- кубічне кільце та $\theta = \dfrac{\alpha + \beta \omega_1}{p}$ при $\beta \neq 1$. Тоді кільце не допускає розширення вигляду $\tilde{\Lambda} = \left< 1,\theta,\theta^2\right>$.
\end{lemma}

\textcolor{red}{Надалі припускаємо, що $\beta = 1$.}

Аналогічно проведемо процедуру для $\omega_2$. Отримуємо:
\begin{align*}
\xi + \eta \cdot \dfrac{\alpha}{p} + \zeta \cdot \dfrac{\alpha^2 - \beta^2 ac}{p^2} = 0 \\
\eta \cdot \dfrac{\beta}{p} + \zeta \cdot \dfrac{2\alpha\beta + \beta^2 b}{p^2} = 0 \\
\zeta \cdot \dfrac{\beta^2 a}{p^2} = 1
\end{align*}

Звідси $\zeta = \dfrac{p^2}{a}$. Отже, $a \in \{1,p,p^2\}$. Далі, $\eta \cdot \dfrac{1}{p} + \dfrac{p^2}{a} \cdot \dfrac{2\alpha + b}{p^2} = 0$. Звідси $\eta = -\dfrac{p(2\alpha+b)}{a}$. Нарешті, $\xi = \dfrac{\alpha^2 + \alpha b + ac}{a}$.

\begin{lemma}
Нехай $\Lambda$ -- кубічне кільце та $\theta = \dfrac{\alpha + \omega_1}{p}$. Тоді кільце не допускає розширення вигляду $\tilde{\Lambda} = \left< 1,\theta,\theta^2\right>$, якщо $a \notin \{1,p,p^2\}$.
\end{lemma}

Розглянемо кожний $a$ окремо.
\subsubsection{$a = 1$} Усі числа $\zeta,\eta,\xi$ -- цілі, тож не потребує додаткових обмежень.

\subsubsection{$a = p$} Тоді $\zeta,\eta$ -- цілі, але $\xi = \dfrac{\alpha^2 + \alpha b + ac}{p}$, тож додатково треба додати обмеження $\alpha^2 + \alpha b + ac \equiv 0 \pmod p$. Маючи умову $a = p$, ми отримуємо
\begin{align*}
\alpha(\alpha+b) \equiv 0 \pmod p \iff \left[ \begin{gathered} \alpha \equiv 0 \pmod p \\ \alpha \equiv -b \pmod p \end{gathered} \right. .
\end{align*}
Якщо $\alpha \equiv 0 \pmod p$, то із системи рівнянь випливає, що $p \mid b,c,d$.

\begin{lemma}
Нехай $\Lambda$ -- кубічне кільце та $a = p$, а також $p \mid b,c,d$. Тоді кільце $\tilde{\Lambda} = \left<1,\theta,\theta^2\right>$ буде розширенням при $\theta = \dfrac{\omega_1}{p}$.
\end{lemma}

Якщо $\alpha \equiv -b \pmod p$, то із третього рівняння системи отримуємо $-2b \equiv 0 \pmod p$. Якщо $p \neq 2$, то тоді $p \mid b$, але тоді знову $\alpha \equiv 0 \pmod p$. Отримуємо такі самі обмеження.

Утім при $\alpha \equiv -b \pmod p$ та при $p = 2$ ситуація змінюється. Залишається варіант -- коли $\alpha = 1$, а тому $b \equiv 1 \pmod 2$. Утім із другого рівняння системи випливає, що $3 + 2c + 2b \equiv 0 \pmod 4$, проте якщо спуститися до $\pmod 2$, ми отримуємо $1 \equiv 0 \pmod 2$, що неможливо. 

\subsubsection{$a = p^2$} Тоді $\zeta$ -- ціле, решта $\eta = -\dfrac{2\alpha + b}{p}$ та $\xi = \dfrac{\alpha^2 + \alpha b + ac}{p^2}$. Отже, маємо два додаткових обмеження:
\begin{align*}
-2\alpha - b \equiv 0 \pmod p \\
\alpha^2 + \alpha b + ac \equiv 0 \pmod {p^2}
\end{align*}
Отримуємо $\alpha \equiv 0 \pmod p$. Внаслідок чого $b \equiv 0 \pmod p$. Всі решта рівняння виконуються.

\begin{lemma}
Нехай $\Lambda$ -- кубічне кільце та $a = p^2$, а також $p \mid b$. Тоді кільце $\tilde{\Lambda} = \left<1,\theta,\theta^2\right>$ буде розширенням при $\theta = \dfrac{\omega_1}{p}$.
\end{lemma}

Нормалізуємо новоутворене кубічне кільце $\tilde{\Lambda}$. Маємо наступне:
\begin{align*}
\theta \cdot \theta^2 = \theta^3 = \det [\theta] - M_{2 \times 2}[\theta] \cdot \theta + \tr[\theta] \cdot \theta^2
\end{align*}
Оберемо новий базис $\left\{ 1,\ \theta-\tr[\theta],\ \theta^2 + M_{2 \times 2}[\theta] \right\}$. Для зручності позначу цей базис за $\{1,\tilde{\omega}_1,\tilde{\omega}_2\}$. У результаті за процедурою нормалізації базису вище отримуємо
\begin{align*}
\tilde{\omega}_1 \cdot \tilde{\omega}_2 = \det [\theta] - \tr [\theta] M_{2 \times 2}[\theta]
\end{align*}
Визначимо, які числа $\tilde{a},\tilde{b},\tilde{c},\tilde{d}$ визначені в кільці $\tilde{\Lambda} = \left< 1, \tilde{\omega}_1,\tilde{\omega}_2 \right>$.
\begin{align*}
\tilde{\omega}_1^2 = (\theta - \tr [\theta])^2 = \theta^2 - 2 \theta \tr [\theta] + (\tr [\theta])^2 \\ = \tilde{b} (\theta - \tr [\theta]) + \tilde{a} (\theta^2 + M_{2 \times 2}[\theta]) - \tilde{a} \tilde{c} = \tilde{b} \tilde{\omega}_1 + \tilde{a} \tilde{\omega}_2 - \tilde{a} \tilde{c}
\end{align*}
Звідси випливає, що $\tilde{a} = 1,\ \tilde{b} = - 2 \tr [\theta]$ та $\tilde{c} = \tr^2 [\theta] + M_{2 \times 2}[\theta]$. Далі,
\begin{align*}
\tilde{\omega}_1 \cdot \tilde{\omega}_2 = (\theta - \tr [\theta])(\theta^2 + M_{2 \times 2}[\theta]) =\\ = (\det [\theta] - M_{2 \times 2}[\theta] \theta + \tr [\theta] \theta^2) + M_{2 \times 2}[\theta] \theta - \tr[\theta] \theta^2 - \tr [\theta] M_{2 \times 2}[\theta] \\
= \det [\theta] - \tr [\theta] M_{2 \times 2}[\theta] = \tilde{a} \tilde{d}
\end{align*}
Звідси $\tilde{d} = \det [\theta] - \tr [\theta] M_{2 \times 2}[\theta]$.

Отже, кубічне кільце $\tilde{\Lambda}$ задається числами
\begin{align*}
(\tilde{a},\tilde{b},\tilde{c},\tilde{d}) = (1,\ -2 \tr [\theta],\ \tr^2[\theta] + M_{2 \times 2}[\theta],\ \det [\theta] - \tr[\theta] M_{2 \times 2}[\theta]).
\end{align*}

\subsection{$\theta^3 \notin \tilde{\Lambda}$}
Увесь цей час, маючи кільце $\Lambda$, ми розширювали кільце $\tilde{\Lambda}$ таким чином, що $\{1,\theta,\theta^2\}$ був обов'язково базисом. Чи можна розширити кільце так, щоб в базисі лише один елемент не потрапляв у $\tilde{\Lambda}$?

\subsubsection{Заміна $\omega_2$} Припустимо, що $\{1,\omega_1,\theta\}$ буде базисом кільця $\tilde{\Lambda}$, де $\theta \notin \Lambda$. Елементи кільця $\tilde{\Lambda}$ матимуть вигляд $x + y \omega_1 + z \theta$. Оскільки $\omega_2 \in \Lambda$, то елемент $\theta$ можна подати в вигляді
\begin{align*}
\theta = \dfrac{\alpha + \beta \omega_1 + \omega_2}{p}
\end{align*}
Для кубічного кільця необхідно, щоб $\omega_1 \theta$ та $\theta^2$ потрапляли в $\tilde{\Lambda}$.
\begin{align*}
\omega_1 \theta & = \omega_1 \dfrac{\alpha + \beta \omega_1 + \omega_2}{p} = \dfrac{ad + \alpha \omega_1 + \beta (b\omega_1 + a\omega_2 - ac)}{p} \\
& = x + y \omega_1 + z \dfrac{\alpha + \beta \omega_1 + \omega_2}{p}
\end{align*}
Шукаючи числа $x,y,z$, отримуємо наступне:
\begin{align*}
x = \dfrac{ad - \beta ac - \alpha \beta a}{p} \\
y = \dfrac{\alpha + \beta b - \beta^2 a}{p} \\
z = \beta a
\end{align*}
Оскільки $x,y \in \mathbb{Z}$, то звідси маємо
\begin{equation*}
\begin{cases}
ad - \beta ac - \alpha \beta a & \equiv 0 \pmod p \\
\alpha + \beta b - \beta^2 a & \equiv 0 \pmod p
\end{cases}
\end{equation*}
Проведемо схожу процедуру з $\theta^2$.
\begin{align*}
\theta^2 & = \dfrac{\alpha^2 + \beta^2 (b \omega_1 + a \omega_2 - ac) + (d \omega_1 + c \omega_2 - bd) + 2 \alpha \beta \omega_1 + 2 \alpha \omega_2 + 2 \beta ad }{p^2} \\
& = x + y \omega_1 + z \dfrac{\alpha + \beta \omega_1 + \omega_2}{p}
\end{align*}
Шукаючи коефіцієнти, отримуємо
\begin{align*}
x = \dfrac{-\alpha^2 - \beta^2 ac - bd + 2 \beta a d - \alpha c - \alpha \beta^2 a}{p^2} \\
y = \dfrac{\beta^2 b + d - \beta c - \beta^3 a}{p^2} \\
z = \dfrac{c +\beta^2 a + 2\alpha}{p}
\end{align*}
Оскільки знову $x,y,z \in \mathbb{Z}$, маємо отримати
\begin{equation*}
\begin{cases}
c +\beta^2 a + 2\alpha & \equiv 0 \pmod p \\
\beta^2 b + d - \beta^2 c - \beta^3 a & \equiv 0 \pmod {p^2} \\
-\alpha^2 - \beta^2 ac - bd + 2 \beta a d - \alpha c - \alpha \beta^2 a & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation*}
Разом маємо систему конгруентних рівнянь:
\begin{equation}
\label{system_of_congruent_equations}
\begin{cases}
ad - \beta ac - \alpha \beta a & \equiv 0 \pmod p \\
\alpha + \beta b - \beta^2 a & \equiv 0 \pmod p \\
c +\beta^2 a + 2\alpha & \equiv 0 \pmod p \\
\beta^2 b + d - \beta c - \beta^3 a & \equiv 0 \pmod {p^2} \\
-\alpha^2 - \beta^2 ac - bd + 2 \beta a d - \alpha c - \alpha \beta^2 a & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation}

\begin{remark}
Аналогічно нам досить розглядати лише $\alpha,\beta$ з точністю до $\pmod p$.
\end{remark}

Нормалізуємо розширене кільце $\tilde{\Lambda} = \left<1, \omega_1, \theta\right>$. Пригадаємо, що
\begin{align*}
\omega_1 \theta = \beta a \cdot 1 + \dfrac{\alpha + \beta b - \beta^2 a}{p} \cdot \omega_1 + \dfrac{-\beta a c + ad - \beta \alpha a}{p} \cdot \theta
\end{align*}
Тоді нормальним базисом буде система $\left\{ 1,\omega_1-\beta a, \theta - \dfrac{\alpha+\beta b - \beta^2 a}{p} \right\} = \left\{1,\tilde{\omega}_1,\tilde{\omega}_2\right\}$. Знайдемо оновлені значення  $\tilde{a},\tilde{b},\tilde{c},\tilde{d}$ нового кільця.
\begin{align*}
\tilde{\omega}_1^2 = \tilde{b} \tilde{\omega}_1 + \tilde{a} \tilde{\omega}_2 - \tilde{a} \tilde{c}
\end{align*}
Підставимо $\tilde{\omega}_1,\tilde{\omega}_2$ до цього рівняння -- отримуємо
\begin{align*}
(b \omega_1 + a \omega_2 - ac) - 2 \beta a \omega_1 + \beta^2 a^2 \\ = \tilde{b} (\omega_1 - \beta a) + \tilde{a} \left( \dfrac{\alpha + \beta \omega_1 + \omega_2}{p} - \dfrac{\alpha + \beta b - \beta^2 a}{p}\right) - \tilde{a} \tilde{c} 
\end{align*}
Залишилось прирівняти коефіцієнти. У результаті виникне наступне:
\begin{align*}
\tilde{a} = p a \\
\tilde{b} = b - 3 \beta a \\
\tilde{c} = \dfrac{c - 2 \beta b + 3 \beta^2 a}{p}
\end{align*}
Число $\tilde{d}$ знайдемо із даного добутку:
\begin{align*}
\tilde{\omega}_1 \tilde{\omega}_2 = \tilde{a} \tilde{d}
\end{align*}
За процедурою нормалізації ліва частина матиме такий вигляд:
\begin{align*}
\dfrac{-\beta a c + ad - \beta \alpha a}{p}+ \dfrac{\alpha + \beta b - \beta^2 a}{p} \cdot \beta a = \tilde{a} \tilde{d}
\end{align*}
У результаті отримуємо наступне:
\begin{align*}
\tilde{d} = \dfrac{-\beta^3 a + \beta^2 b - \beta c + d}{p^2}
\end{align*}

Отже, $\left( \tilde{a},\tilde{b},\tilde{c},\tilde{d} \right) = \left( pa,\ b - 3 \beta a,\ \dfrac{c - 2 \beta b + 3 \beta^2 a}{p},\ \dfrac{-\beta^3 a + \beta^2 b - \beta c + d}{p^2} \right)$.

\begin{remark}
Число $\tilde{c} \in \mathbb{Z}$, оскільки зі системи випливає, що чисельник
\begin{align*}
c - 2 \beta b + 3 \beta^2 a = (c + \beta^2 a + 2\alpha) - 2(\alpha + \beta b- \beta^2 a) \equiv 0 \pmod p
\end{align*}
Також число $\tilde{d} \in \mathbb{Z}$, оскільки із системи
\begin{align*}
-\beta^3 a + \beta^2 b - \beta c + d \equiv 0 \pmod {p^2}
\end{align*} 
\end{remark}

\subsubsection{Заміна $\omega_1$} Припустимо, що $\{1,\theta,\omega_2\}$ буде базисом кільця $\tilde{\Lambda}$, де $\theta \notin \Lambda$. Цього разу елемент $\theta$ матиме вигляд:
\begin{align*}
\theta = \dfrac{\alpha + \omega_1 + \beta \omega_2}{p}
\end{align*}

Підемо іншим шляхом. Ми можемо замінити базис $\{1,\omega_1,\omega_2\}$ кільця $\Lambda$ на базис $\{1,\omega_2,\omega_1\}$ кільця $\Lambda$, а далі розширитися до кільця $\tilde{\Lambda}$, замінивши $\omega_1$ на елемент $\theta = \dfrac{\alpha + \beta \omega_2 + \omega_1}{p}$ уже за готовим алгоритмом.

При заміні базиса $\{1,\omega_1,\omega_2\}$ на $\{1,\omega_1',\omega_2'\} = \{1,\omega_2,\omega_1\}$ четвірка $(a,b,c,d)$ кільця $\Lambda$ заміниться на четвірку $(a',b',c',d') = (d,c,b,a)$. Дійсно,
\begin{align*}
\omega_1'^2 = b' \omega_1' + a' \omega_2' - a' c' \\
d \omega_1 + c \omega_2 - bd = b' \omega_2 + a' \omega_1 - a' c'
\end{align*}
Отже, звідси $b' = c,\ a' = d$. Аналогічно, розписавши
\begin{align*}
\omega_2'^2 = d' \omega_1' + c' \omega_2' - b' d',
\end{align*}
ми здобудемо $c' = b,\ d' = a$.

Таким чином, необхідно буде розв'язати ту саму систему рівнянь \eqref{system_of_congruent_equations}, замінивши літери $a$ на $d$ і навпаки та $b$ на $c$ і навпаки.

Нормальним базисом буде система $\left\{ 1,\ \omega_2 - \beta d, \theta - \dfrac{\alpha + \beta c - \beta^2 d}{p} \right\}$, а четвірка кільця $\tilde{\Lambda}$ має такий вигляд: \\ $\left( \tilde{a},\tilde{b},\tilde{c},\tilde{d}\right) = \left( pd,\ c - 3 \beta d,\ \dfrac{b-2\beta c + 3 \beta^2 d}{p},\ \dfrac{-\beta^3 d + \beta^2 c - \beta b + a}{p^2}\right)$.

\iffalse
\section{Розв'язання системи конгруентних рівнянь}
\subsection{Випадок, коли $p$ -- просте число, де $p \neq 3$}

Маємо рівняння
\begin{align*}
bt \equiv p-3 \pmod p.
\end{align*}
Дане рівняння має розв'язок $\iff \gcd(b,p) \mid p-3$. Оскільки $p$ -- просте, то $\gcd(b,p) \in \{1,p\}$. Якби $\gcd(b,p) = p$, то отримали би $p \mid p-3$, що неможливо.

Отже, якщо $p \nmid b$, то вищезгадане рівняння має розв'язок. При цьому оскільки $\gcd(b,p) = 1$, то рівняння має лише єдиний розв'язок вигляду
\begin{align*}
t \equiv -3b^{-1} \pmod p
\end{align*}

Єдиний можливий розв'язок необхідно підставити в інші два рівняння системи. Для початку піднімемо розв'язок до $\pmod {p^2}$ -- отримуємо наступне:
\begin{align*}
t \equiv -3b^{-1} + kp \pmod {p^2},
\end{align*}
де $k \in \{0,1,\dots,p-1\}$. Щойно отримане $t$ підставимо в друге рівняння:
\begin{align*}
act^2 + 2bt + 3 & \equiv ac(-3b^{-1} + kp)^2 + 2b(-3b^{-1}+kp) + 3 \\
& \equiv ac(k^2p^2 - 6kpb^{-1} + 9b^{-2}) + 2b(kp - 3b^{-1}) + 3 \\
& \equiv -6kp acb^{-1} + 9 acb^{-2} + 2bkp - 3 \equiv 0 \pmod {p^2}
\end{align*}
Домножимо обидві частини на $b^2$ -- отримуємо:
\begin{align*}
-6kpabc + 9ac + 2b^3kp - 3b^2 & \equiv 2bkp(b^2 - 3ac) - 3(b^2 - 3ac) \\
& \equiv (b^2 - 3ac)(2bkp -1) \equiv 0 \pmod {p^2}
\end{align*}
Зауважимо, що $\gcd(2bkp-1, p^2) = 1$, тому можемо скоротити рівняння:
\begin{align*}
b^2 - 3 ac \equiv 0 \pmod {p^2}
\end{align*}

Отже, якщо $p \nmid b$, а також $b^2 \equiv 3ac \pmod {p^2}$, то наш єдиний розв'язок задовольняє систему із останніх двох рівнянь. Причому $t \equiv -3b^{-1} + kp \pmod {p^2}$ не залежить від параметру $k$.

Зробимо аналогічно для першого рівняння системи, тільки замість $\pmod {p^3}$ розглядається $\pmod {p^2}$ -- маємо:
\begin{multline*}
a^2 dt^3 + act^2 + bt + 1 \\ \equiv a^2 d (-3b^{-1} + kp)^3 + ac (-3b^{-1} + kp)^2 + b (-3b^{-1} + kp) + 1 \\
\equiv a^2 d (-27 b^{-3} + 27kp b^{-2}) + ac (9b^{-2} - 6b^{-1}kp) + b(-3b^{-1} + kp) + 1 \\ \equiv 0 \pmod {p^2}
\end{multline*}
Домножимо обидві частини рівності на $b^{-3}$ -- із урахуванням рівності $b^2 \equiv 3 ac \pmod {p^2}$ отримуємо:
\begin{multline*}
a^2 d(-27 + 27 kpb) + ac(9b - 6b^2 kp) + b^4(-3b^{-1} + kp) + b^3 \\
\equiv -27a^2d  + 27 a^2 b d k p + 9abc - 6 ab^2 c kp - 3b^3 + b^4 kp + b^3 \\
\equiv -27a^2d + 27 a^2 b d k p + 3b^3 - 2 b^4 kp - 3b^3 + b^4 kp + b^3 \\
\equiv -27a^2d (1 - bkp) + b^3(1 - bkp) \equiv (1-bkp)(b^3 - 27a^2d) \equiv 0 \pmod {p^2}
\end{multline*}
Зауважимо, що $\gcd(1-bkp, p^2) = 1$, тому можемо скоротити рівняння:
\begin{align*}
b^3 - 27a^2 d \equiv 0 \pmod {p^2}
\end{align*}

Нарешті, розглянемо перше рівняння:
\begin{align*}
a^2 dt^3 + act^2 + bt + 1 \equiv 0 \pmod {p^3}
\end{align*}
Оскільки $t \equiv -3b^{-1} + kp \pmod {p^2}$ буде розв'язком системи останніх двох рівнянь при вищезгаданих умов, причому нема залежності від $k$, то для спрощення оберемо $t \equiv -3b^{-1} \pmod {p^2}$. Піднімемо це рівняння до $\pmod {p^3}$ -- отримуємо наступне:
\begin{align*}
t \equiv -3b^{-1} + lp^2 \pmod {p^3},
\end{align*} 
де $l \in \{0,1,\dots,p-1\}$. Щойно отримане $t$ підставимо в перше рівняння:
\begin{multline*}
a^2 dt^3 + act^2 + bt + 1 \\ 
\equiv a^2 d (-3b^{-1} + lp^2)^3 + ac(-3b^{-1} + lp^2)^2 + b(-3b^{-1} + lp^2) + 1 \\
\equiv a^2d (-27b^{-3} + 27b^{-2} lp^2) + ac (9b^{-2} - 6 l b^{-1} p^2) + b (-3b^{-1} + lp^2) + 1 \\ 
\equiv 0 \pmod {p^3}
\end{multline*}
Домножимо обидві частини рівності на $b^3$ -- отримуємо:
\begin{multline*}
a^2d (-27 + 27b lp^2) + ac (9b - 6 l b^2 p^2) + (-3 b^3 + lp^2 b^4) + b^3 \\ 
\equiv 0 \pmod {p^3}
\end{multline*}
Пригадаємо, що необхідно $b^2 \equiv ac \pmod {p^2}$, а також $27a^2 d \equiv b^3 \pmod {p^2}$. Підмінаючи до $\pmod {p^3}$, отримуємо:
\begin{align*}
3ac \equiv b^2 + \xi p^2 \pmod {p^3} \\
27a^2 d \equiv b^3 + \eta p^2 \pmod {p^3},
\end{align*}
де $\xi,\eta \in \{0,1,\dots,p-1\}$. Внаслідок чого
\begin{multline*}
27 a^2d (-1+ b lp^2) + 3ac (3b - 2 l b^2 p^2) + (-3 b^3 + lp^2 b^4) + b^3 \\ \equiv (b^3 + \eta p^2)(-1 + b lp^2) + (b^2 + \xi p^2)(3b - 2 l b^2 p^2) + (-3 b^3 + lp^2 b^4) + b^3 \\ \equiv -b^3 + b^4 lp^2 - \eta p^2 + 3b^3 - 2b^4 lp^2 + 3 \xi p^2 b - 3b^3 + b^4 lp^2 + b^3 \\
\equiv - \eta p^2 + 3\xi p^2 b \equiv 0 \pmod {p^3}
\end{multline*}
Отже, ми отримали наступне:
\begin{align*}
\eta p^2 \equiv 3 \xi p^2 b \pmod {p^3}
\end{align*}
Тоді зауважимо, що
\begin{align*}
27 a^2 d \equiv b^3 + \eta p^2 \equiv b^3 + 3 \xi p^2 b \equiv b^3 + 3b(3ac - b^2) \equiv -2b^3 + 9abc \pmod {p^3}
\end{align*}
Підсумуємо:

\begin{lemma}
\label{system_when_p_is_prime_and_bigger_than_three}
Нехай $p > 3$ -- просте число або $p = 2$. Припустимо, що $p \nmid b,\ b^2 \equiv 3ac \pmod {p^2}$, а також $27a^2d \equiv 9abc - 2b^3 \pmod {p^3}$. Тоді система конгруентних рівнянь \eqref{system_congruent_equations} має єдиний розв'язок $t \equiv -3 b^{-1} \pmod {p}$, де число $b^{-1}$ -- оборотний елемент класу лишків $\mathbb{Z}_p$ відносно множення.
\end{lemma}

Пригадаємо, що кубічному кільцю відповідає кубічна форма, дискримінант якої обчислюється за формулою
\begin{equation}
\Delta = b^2 c^2 - 4c^3 a - 4 b^3 d + 18 abcd - 27a^2d^2
\end{equation}
Сприймаючи дискримінант за функцію від чотирьох змінних, знайдемо частинну похідну за аргументом $d$:
\begin{align*}
\Delta'_d = -4b^3 + 18 abc - 54a^2 d
\end{align*}
Тоді умову $27a^2d \equiv 9abc - 2b^3 \pmod {p^3}$ після множення на $2$ можна записати таким чином:
\begin{align*}
\Delta'_d \equiv 0 \pmod {p^3}
\end{align*}

\subsection{Випадок, коли $p = 3$}. Третє рівняння системи спроститься до вигляду
\begin{align*}
bt \equiv 0 \pmod 3
\end{align*}

Припустимо, що $3 \nmid b$. Тоді система може має єдиний розв'язок $t \equiv 0 \pmod 3$. Якщо піднятися до $\pmod 9$, то в нас існують три опції:
\begin{align*}
t \equiv 0 \pmod 9,\qquad t \equiv 3 \pmod 9,\qquad t \equiv 6 \pmod 9
\end{align*}
Якщо $t \equiv 0 \pmod 9$, то із другого рівняння випливає, що $3 \equiv 0 \pmod 9$, що неможливо. Якщо $t \equiv 3 \pmod 9$, то із першого рівняння випливає, що
\begin{align*}
3b \equiv 8 \pmod 9
\end{align*}
Оскільки $\gcd(3,9) = 3 \nmid 8$, то розв'язків не має -- неможливо. Нарешті, якщо $t \equiv 6 \pmod 9$, то із першого рівняння випливає, що
\begin{align*}
6b \equiv 8 \pmod 9
\end{align*}
Аналогічними міркуваннями розв'язків нема -- неможливо.

\begin{lemma}
Система конгруентних рівнянь \eqref{system_congruent_equations} не має розв'язків при $3 \nmid b$.
\end{lemma}

Надалі розглядається випадок $3 \mid b$. Третє рівняння автоматично виконується, тому залишається система
\begin{equation}
\label{system_congruent_equations}
    \begin{cases}
      a^2 d t^3 + ac t^2 + bt + 1 & \equiv 0 \pmod {27} \\
      act^2 + 2bt + 3 & \equiv 0 \pmod {9} 
    \end{cases}\,.
\end{equation}
Тимчасово опустимося до системи рівнянь за $\pmod 3$. Зважаючи на умову $3 \mid b$, маємо
\begin{equation*}
\label{system_congruent_equations}
    \begin{cases}
      a^2 d t^3 + ac t^2 + 1 & \equiv 0 \pmod {3} \\
      act^2 & \equiv 0 \pmod {3} 
    \end{cases}\,.
\end{equation*}

Припустимо, що $3 \nmid ac$. Тоді із другого рівняння отримуємо $t \equiv 0 \pmod 3$. Але, підставляючи в перше рівняння, матимемо $1 \equiv 0 \pmod 3$, що неможливо.

Отже, надалі $3 \mid ac$. Звідси ми маємо $3 \mid a$ або $3 \mid c$.

Якщо $3 \mid a$, то знову $1 \equiv 0 \pmod 3$ із першого рівняння, що неможливо.

\begin{lemma}
Система конгруентних рівнянь \eqref{system_congruent_equations} не має розв'язків при $3 \nmid ac$ або $3 \mid a$.
\end{lemma}

Отже, залишається випадок, коли $3 \mid c$. Значить, перше рівняння дає
\begin{align*}
a^2 dt^3 \equiv 2 \pmod 3
\end{align*}
Зауважимо, що розв'язок існує при $3 \nmid a^2d$.
\begin{corollary}
Система конгруентних рівнянь \eqref{system_congruent_equations} не має розв'язків при $3 \mid d$.
\end{corollary}

У нас існують два варіанти за $\pmod 3$: $a^2 d \in \{1,2\}$.

Нехай $a^2 d \equiv 1 \pmod 3$, тоді маємо $t^3 \equiv 2 \pmod 3$, внаслідок чого отримуємо $t \equiv 2 \pmod 3$ -- єдиний кандидат на розв'язок системи \eqref{system_congruent_equations} з урахуванням певних умов.

Нехай $a^2 d \equiv 2 \pmod 3$, тоді маємо $2t^3 \equiv 2 \pmod 3$, внаслідок чого отримуємо $t \equiv 1 \pmod 3$ -- також єдиний кандидат.

Умову $a^2 d \equiv 1 \pmod 3$ можна трошки детально розписати. Уже відомо, що $3 \nmid d$, а тому звідси $d \in \{1,2\}$, якщо брати за $\pmod 3$. Якщо $d = 2$, то звідси $2a^2 \equiv 1 \pmod 3$, або $a^2 \equiv 2 \pmod 3$. Проте зазначимо, що $2$ не є квадратичним лишком $\pmod 3$, тому розв'язків відносно $a$ не має. Водночас якщо $d = 1$, то $a^2 \equiv 1 \pmod 3$, де існує розв'язок.

Аналогічними міркуваннями розглянемо $a^2 d \equiv 2 \pmod 3$. Цього разу при $d = 1$ не знайдеться розв'язку відносно $a$, коли водночас при $d = 2$ розв'язок існує.

Підіб'ємо певний підсумок: 

\begin{proposition}
Нехай $3 \nmid a,\ 3 \mid b,\  3 \mid c$. Тоді із системи рівнянь \eqref{system_congruent_equations} випливають можливі розв'язки: 
\begin{enumerate}
\item $t \equiv 2 \pmod 3$ за умовою, що $d \equiv 1 \pmod 3$;
\item $t \equiv 1 \pmod 3$ за умовою, що $d \equiv 2 \pmod 3$.
\end{enumerate}
\end{proposition}

\subsubsection*{Експериментальні результати}
Піднімаючись до $\!\! \pmod 27$, ми маємо вісімнадцять потенційних розв'язків у залежності від параметра $d$: \begin{align*}
t \in \{1,4,7,10,13,16,19,22,25\} \text{ при } d \equiv 2 \pmod 3 \\
t \in \{2,5,8,11,14,17,20,23,26\} \text{ при } d \equiv 1 \pmod 3
\end{align*}

\subsubsection*{Теоретичний пошук}
Маючи накладені умови з твердження, перевіримо, чи будуть вищезгадані $t$ дійсно розв'язками системи.

\subsubsection{Випадок $t \equiv 2 \pmod 3$}
Отже, в цьому випадку $a^2d \equiv 1 \pmod 3$.

Спочатку піднімемося до $\pmod 9$. Тоді маємо три потенційних кандидати: $t \in \{2,5,8\}$.

Нехай $t \equiv 2 \pmod 9$. Маємо наступне:
\begin{equation*}
    \begin{cases}
      8 a^2 d  + 4 ac + 2b + 1 & \equiv 0 \pmod {27} \\
      4ac + 4b + 3 & \equiv 0 \pmod {9} 
    \end{cases}\,.
\end{equation*}
Оскільки $a^2 d \equiv 1 \pmod 3$, то звідси за $\pmod 9$ маємо $a^2 d \in \{1,4,7\}$. Якщо систему рівнянь спустити до $\pmod 9$, отримуємо три варіанти
\begin{align*}
    \begin{cases}
      8  + 4 ac + 2b + 1 & \equiv 0 \pmod {9} \\
      4ac + 4b + 3 & \equiv 0 \pmod {9} 
    \end{cases}\, \\    \begin{cases}
      32  + 4 ac + 2b + 1 & \equiv 0 \pmod {9} \\
      4ac + 4b + 3 & \equiv 0 \pmod {9} 
    \end{cases}\, \\     \begin{cases}
      49  + 4 ac + 2b + 1 & \equiv 0 \pmod {9} \\
      4ac + 4b + 3 & \equiv 0 \pmod {9} 
    \end{cases}\,.
\end{align*}
Розглянемо першу систему. Ми отримуємо $4ac \equiv -2b \pmod 9$. Значить, звідси
\begin{align*}
4ac + 4b + 3 \equiv 2b + 3 \equiv 0 \pmod 9
\end{align*}
Останнє можливе лише при $b \equiv 3 \pmod 9$. Також маємо $4ac \equiv -6 \equiv 3 \pmod 9$.
\fi

\newpage

\chapter{Розв'язання системи конгруентних рівнянь}\label{chaper2}
Нехай $\Lambda$ -- немаксимальне кубічне кільце. Розглянемо елемент $\theta \notin \Lambda$. Хочемо розширити до кубічного кільця $\tilde{\Lambda} = \Lambda[\theta] \supsetneq \Lambda$.

\section{Система рівнянь при $\theta^3 \notin \tilde{\Lambda}$}
Необхідно розв'язати систему конгруентних рівнянь \ref{system_of_congruent_equations}:
\begin{equation}
\begin{cases}
ad - \beta ac - \alpha \beta a & \equiv 0 \pmod p \\
\alpha + \beta b - \beta^2 a & \equiv 0 \pmod p \\
c +\beta^2 a + 2\alpha & \equiv 0 \pmod p \\
\beta^2 b + d - \beta c - \beta^3 a & \equiv 0 \pmod {p^2} \\
-\alpha^2 - \beta^2 ac - bd + 2 \beta a d - \alpha c - \alpha \beta^2 a & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation}

\subsection{$p > 3$}
Припустимо, що $p \mid a$. Тоді система \eqref{system_of_congruent_equations} спроститься до вигляду:
\begin{equation}\label{system_case_when_a_is_zero}
\begin{cases}
\alpha + \beta b & \equiv 0 \pmod p \\
c + 2\alpha & \equiv 0 \pmod p \\
\beta^2 b + d - \beta c - \beta^3 a & \equiv 0 \pmod {p^2} \\
-\alpha^2 - \beta^2 ac - bd + 2 \beta a d - \alpha c - \alpha \beta^2 a & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation}

Зауважимо, що $2\alpha \equiv -c \pmod p$. Завдяки умові $p \neq 2$, дане рівняння має розв'язок відносно $\alpha$. Домножуючи перше рівняння на $2$, отримуємо $2 \alpha + 2 \beta b \equiv 0 \pmod p$, тобто $-c + 2 \beta b \equiv 0 \pmod p$. Отже, $2 \beta b \equiv c \pmod p$. Останнє має розв'язок відносно $\beta$ тоді й лише тоді, коли $\gcd(2b,p) \mid c$.

Припустимо, що $p \nmid b$. Тоді останнє згадане рівняння має єдиний розв'язок відносно $\beta$. Підставимо значення $\alpha,\beta$ (точніше кажучи, $2\beta b$ та $2\alpha$) в останні два рівняння системи \eqref{system_case_when_a_is_zero}. Для цього передостаннє рівняння домножимо на $8b^3$, а останнє -- на $8b^2$. У результаті чого отримуємо таку систему:
\begin{equation*}
\begin{cases}
2 \beta b & \equiv c \pmod p \\
2 \alpha & \equiv -c \pmod p \\
-2 b^2 c^2 + 8b^3 d - ac^3 & \equiv 0 \pmod {p^2} \\
- ac^3 - 8b^3d + 8abcd + 2b^2c^2 & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation*}
Із передостаннього можна виразити $8b^3d - 2b^2c^2$ та підставити до останнього, щоб спростити систему.

Коректніше було б підняти $2\alpha$ до $\pmod {p^2}$, тобто записати в такому вигляді:
\begin{align*}
2 \alpha \equiv -c \textcolor{red}{+ p k} \pmod {p^2},
\end{align*}
де $k \in \{0,1,\dots,p-1\}$. Проте оскільки $\alpha,\beta \in \{0,1,\dots,p-1\}$, то можна не записувати доданок $\textcolor{red}{+pk}$. Аналогічне зауваження стосується до $2 b\beta$.

\begin{proposition}
\label{extension_for_p_mid_a_p_nmid_b_when_p_bigger_than_3}
Нехай задано кільце $\Lambda$ при $p \mid a,\ p \nmid b$.\\
Кільце $\Lambda$ допускає розширення елементом $\theta = \dfrac{\alpha + \beta \omega_1 + \omega_2}{p}$ $\iff \\ \begin{cases} -2b^2c^2 + 8b^3d - ac^3 \equiv 0 \pmod {p^2} \\
-ac^3 + 4abcd \equiv 0 \pmod {p^2}
\end{cases}$.\\
Числа $\alpha,\beta$ шукаються в рівняннях $\begin{cases}
2 \beta b & \equiv c \pmod p \\
2 \alpha & \equiv -c \pmod p \end{cases}$.
\end{proposition}

Припустимо, що $p \mid b$. Тоді, повернувшись до \eqref{system_case_when_a_is_zero} та до рівняння $2\beta b \equiv c \pmod {p}$, отримуємо $p \mid c$, а внаслідок чого $\alpha = 0$. Якщо третє рівняння системи спустити до $\pmod p$, то ми отримуємо $d \equiv 0 \pmod p$. У результаті залишається розв'язати таке рівняння:
\begin{align*}
\beta^2 b + d - \beta^2 c - \beta^3 a \equiv 0 \pmod {p^2}
\end{align*}
Оскільки відомо, що $p \mid a,b,c,d$, а тому $a = p \tilde{a},\ b = p \tilde{b},\ c = p \tilde{c},\ d = p \tilde{d}$, то можна рівняння переписати таким чином:
\begin{align*}
-\beta^3 \tilde{a} + \beta^2 \tilde{b} - \beta^2 \tilde{c} + \tilde{d} \equiv 0 \pmod p
\end{align*}
Якщо $\tilde{a} \equiv 0 \pmod p$, отримуємо спрощену версію рівняння:
\begin{align*}
(\tilde{b} - \tilde{c})\beta^2 + \tilde{d} \equiv 0 \pmod p.
\end{align*}
Чи існує розв'язок рівняння, залежить від символа Лежандра $\left( \dfrac{\tilde{d}}{p} \right)$.

Нехай тепер $\tilde{a} \not\equiv 0 \pmod p$. Маємо
\begin{align*}
-\beta^3 + (\tilde{b} -\tilde{c}) a^{-1} \beta^2  + \tilde{d} \tilde{a}^{-1} \equiv 0 \pmod p
\end{align*}
Задача полягає в розв'язанні кубічного рівняння. Його можна звести до кубічного рівняння з лінійним доданком, якщо провести заміну $\beta = y + \gamma$. Такий $\gamma$ існуватиме лише при $p \neq 3$. Далі залежить від символа Лежандра $\left( \dfrac{\Delta}{p} \right)$, де $\Delta$ -- дискримінант рівняння $y^3 + uy + v = 0$.

Якщо $p = 3$, то виникне $\tilde{b} \equiv \tilde{c} \pmod 3$, внаслідок чого
\begin{align*}
\beta^3 \equiv (\tilde{d} \tilde{a}^{-1}) \pmod 3
\end{align*}
Дане рівняння має розв'язок $\iff (\tilde{d} \tilde{a}^{-1})^{2} \equiv 1 \pmod 3$. Іншими словами, $\tilde{d}^2 \equiv \tilde{a}^2 \pmod 3$.

\textcolor{red}{Надалі $p \nmid a$}. Маємо систему
\begin{equation*}
\begin{cases}
d - \beta c - \alpha \beta & \equiv 0 \pmod p \\
\alpha + \beta b - \beta^2 a & \equiv 0 \pmod p \\
c +\beta^2 a + 2\alpha & \equiv 0 \pmod p \\
\beta^2 b + d - \beta c - \beta^3 a & \equiv 0 \pmod {p^2} \\
-\alpha^2 - \beta^2 ac - bd + 2 \beta a d - \alpha c - \alpha \beta^2 a & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation*}
Із другого рівняння знайдемо $\alpha$:
\begin{align*}
\alpha \equiv \beta^2 a - \beta b \pmod p
\end{align*}
Підставимо всюди $\alpha$ -- у результаті перше рівняння можна не писати, оскільки виконується четверте рівняння, а разом виникне така система:
\begin{equation*}
%\label{system_of_equations_when_a_nmid_p_and_b_nmid_p}
\begin{cases}
3 \beta^2 a - 2 \beta b + c & \equiv 0 \pmod p \\
-\beta^3 a + \beta^2 b - \beta c + d & \equiv 0 \pmod {p^2} \\
-2 a^2 \beta^4 + 3 a b \beta^3 - 2 a c \beta^2 - b^2 \beta^2 + 2 a d \beta + b c \beta - bd & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation*}
Розпишемо останню рівність таким чином:
\begin{align*}
ab \beta^3 - b^2 \beta^2 + b c \beta - bd - 2a^2 \beta^4 + 2 ab \beta^3 - 2 ac \beta^2 + 2 ad \beta \\
\equiv -2a^2 \beta^4 + 2ab \beta^3 - 2 ac\beta^2 + 2 ad \beta \\
= 2a\beta (-a \beta^3 + b \beta^2 - c \beta + d) \equiv 0 \pmod {p^2}
\end{align*}
Останнє рівняння виконується автоматично. Разом маємо систему:
\begin{equation}
\label{system_of_equations_when_a_nmid_p_and_b_nmid_p}
\begin{cases}
3 \beta^2 a - 2 \beta b + c & \equiv 0 \pmod p \\
-\beta^3 a + \beta^2 b - \beta c + d & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation}

%WRONG
\iffalse
Підставивши $\alpha$ в перше рівняння та знизивши четверте рівняння до $\pmod p$, отримуємо два рівняння, які віднімемо -- у результаті
\begin{align*}
c \beta (\beta - 1) \equiv 0 \pmod p
\end{align*}
Отримали необхідну умову для розв'язання системи. Отже, кільце $\Lambda$ при $p \nmid a,\ p \nmid c$, можливо, розширюється елементами вигляду $\theta = \dfrac{\alpha + \omega_2}{p}$ або $\theta = \dfrac{\alpha + \omega_1 + \omega_2}{p}$. Інших варіантів нема.

\subsection*{$\beta = 0$}
Підставивши дане значення, отримуємо систему:
\begin{equation*}
\begin{cases}
d & \equiv 0 \pmod p \\
\alpha & \equiv 0 \pmod p \\
c & \equiv 0 \pmod p \\
d & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation*}
Оскільки ми розглядали кільце $\Lambda$, де $p \nmid c$, то цей випадок не підходить.

\subsection*{$\beta = 1$}
Підставимо це значення:
\begin{equation*}
\begin{cases}
d - c - \alpha & \equiv 0 \pmod p \\
\alpha & \equiv a - b \pmod p \\
c + a + 2\alpha & \equiv 0 \pmod p \\
 b + d - c - a & \equiv 0 \pmod {p^2} \\
-\alpha^2 - ac - bd + 2 \beta a d - \alpha c - \alpha a & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation*}
Перше рівняння після підставлення $\alpha$ дає $b+d-c-a \equiv 0 \pmod p$, яке можна опустити через четверте рівняння. Також підставимо $\alpha$ в третє рівняння.
\begin{equation*}
\begin{cases}
\alpha & \equiv a - b \pmod p \\
c + 3a - 2b & \equiv 0 \pmod p \\
 b + d - c - a & \equiv 0 \pmod {p^2} \\
-\alpha^2 - ac - bd + 2 \beta a d - \alpha c - \alpha a & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation*}
Залишилося підставити $\alpha \equiv a-b + pm \pmod {p^2}$ в останнє рівняння. Враховуючи решта рівнянь системи, отримуємо наступне:
\begin{align*}
b (a-b+c-d) \equiv 0 \pmod {p^2}
\end{align*}
Отже, останнє рівняння виконується автоматично.

\begin{lemma}
Нехай задано кубічне кільце $\Lambda$ при $p \nmid c$.\\
Кільце $\Lambda$ допускає розширення елементом $\theta = \dfrac{\alpha + \omega_1 + \omega_2}{p}$, де число $\alpha \equiv a -b \pmod p \iff \begin{cases} p \mid c + 3a - 2b \\ p^2 \mid -a+b-c+d \end{cases}$.
\end{lemma}

Тепер розглянемо кільце $\Lambda$ при $p \mid c$. Система рівнянь матиме такий вигляд:
\begin{equation*}
\begin{cases}
d - \beta^3 a + \beta^2 b & \equiv 0 \pmod p \\
\alpha & \equiv \beta^2 a - \beta b \pmod p \\
3 \beta^2 a & \equiv 2 \beta b \pmod p \\
\beta^2 b + d - \beta^2 c - \beta^3 a & \equiv 0 \pmod {p^2} \\
-\alpha^2 - \beta^2 a c - bd + 2 \beta a d - \alpha c - \alpha \beta^2 a & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation*}
Із третього рівняння випливає, що або $\beta = 0$ та $\alpha = 0$, або дана змінна шукається за рівнянням $3 \beta a \equiv 2 b \pmod p$. Для другого випадку, якщо друге рівняння домножимо на $9a$, отримуємо
\begin{align*}
9 a \alpha \equiv (3 \beta a)^2 - (3 \beta a) 3 \beta \pmod p \\
9 a \alpha \equiv -2b^2 \pmod p
\end{align*}

Отримали необхідну умову для розв'язання системи. Отже, кільце $\Lambda$ при $p \nmid a,\ p \nmid c$, можливо, розширюється елементами вигляду $\theta = \dfrac{\omega_2}{p}$, або $\theta = \dfrac{\alpha+\beta\omega_1 + \omega_2}{p}$, де числа $\alpha,\beta$ беруться з системи $\begin{cases} 9 a \alpha \equiv -2b^2 \pmod p \\  3 \beta a \equiv 2 b \pmod p \end{cases}$. Інших варіантів нема.

\subsubsection*{$\beta = 0$} Тоді система дає обмеження $p \mid d^2$.
\begin{lemma}
Нехай $\Lambda$ -- кубічне кільце при $p \nmid a,\ p \mid c$.\\
Кільце $\Lambda$ допускає розширення елементом $\theta = \dfrac{\omega_2}{p} \iff p^2 \mid d$. 
\end{lemma}

\subsubsection*{$3 a \beta \equiv 2 b \pmod p$} Третє рівняння домножимо на $27a^2$ -- отримуємо
\begin{align*}
27a^2 d - (3 \beta a)^3 + 3b (3 \beta a)^2 \equiv 0 \pmod p \\
27 a^2 d + 4 b^3 \equiv 0 \pmod p
\end{align*}
Перепишемо систему рівнянь:
\begin{equation*}
\begin{cases}
27 a^2 d + 4 b^3 & \equiv 0 \pmod p \\
9 a \alpha & \equiv -2 b^2 \pmod p \\
3 a \beta & \equiv 2 b \pmod p \\
\beta^2 b + d - \beta^2 c - \beta^3 a & \equiv 0 \pmod {p^2} \\
-\alpha^2 - \beta^2 a c - bd + 2 \beta a d - \alpha c - \alpha \beta^2 a & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation*}
Зробимо підстановку $3a \beta$ та $9a \alpha$ в останні два рівняння. Для цього піднімемо їх:
\begin{align*}
3 a \beta \equiv 2b + pk \pmod {p^2} \\
9a \alpha \equiv -2b^2 + pm \pmod {p^2}
\end{align*}
Четверте рівняння домножимо на $27a^2$ -- після перетворень отримуємо
\begin{align*}
27 a^2 d + 4b^3 - 12 b^2 c \equiv 0 \pmod {p^2}
\end{align*}
Отже, перше рівняння системи можна опустити.

Останнє рівняння домножимо на $81a^2$ -- в результаті:
\begin{align*}
-(9 a \alpha)^2 - 9 a c (3a\beta)^2 - 81 a^2bd + 54 a^2 d (3\beta a) - 9 ac (9 a \alpha) - (9 a \alpha) (3 a \beta)^2 \\ \equiv 0 \pmod {p^2}
\end{align*}
Підставивши та розкривши дужки, одержимо
\begin{align*}
27a^2 bd + 54a^2 dkp - 18ab^2 c + 4b^4 + 8b^3 kp \equiv 0 \pmod {p^2}
\end{align*}
Згрупуємо доданки таким чином:
\begin{align*}
b (27a^2 d + 4b^3) - 18ab^2c + (4b^3 + 27a^2d) 2k p \equiv 0 \pmod {p^2}
\end{align*}
Знаючи, що $27a^2 d + 4b^3 \equiv 12 b^2c \pmod {p^2}$, підставимо це.
\begin{align*}
12b^3 c - 18ab^2c \equiv 0 \pmod {p^2}
\end{align*}
Доданок $12b^2 c \cdot 2kp$ зник, оскільки $pc \equiv 0 \pmod {p^2}$. Отже, система має вигляд:
\begin{equation*}
\begin{cases}
9 a \alpha & \equiv -2 b^2 \pmod p \\
3 a \beta & \equiv 2 b \pmod p \\
27 a^2 d + 4b^3 - 12 b^2 c & \equiv 0 \pmod {p^2} \\
b^2 c (2b - 3a) & \equiv 0 \pmod {p^2}
\end{cases}
\end{equation*}

\begin{proposition}
Нехай $\Lambda$ -- кубічне кільце при $p \nmid a,\ p \mid c$.\\
Кільце $\Lambda$ допускає розширення елементом $\theta = \dfrac{\alpha + \beta \omega_1 + \omega_2}{p}$ при $\begin{cases} 9 a \alpha & \equiv -2 b^2 \pmod p \\
3 a \beta & \equiv 2 b \pmod p \end{cases} \iff \begin{cases} p^2 \mid 27 a^2 d + 4b^3 - 12b^2 c \\ p^2 \mid b^2 c (2b-3a) \end{cases}$.
\end{proposition}
\fi

\subsection{$p = 2$} 
Повторимо кроки для першого частинного випадку. Припустимо, що $2 \mid a$. Тоді система спроститься до вигляду:
\begin{equation*}
\begin{cases}
\alpha + \beta b & \equiv 0 \pmod 2 \\
c & \equiv 0 \pmod 2 \\
\beta^2 b + d - \beta c - \beta^3 a & \equiv 0 \pmod {4} \\
-\alpha^2 - bd - \alpha c - \alpha \beta^2 a & \equiv 0 \pmod {4}
\end{cases}
\end{equation*}
Маємо $\alpha \equiv -\beta b \pmod 2$, а також $2 \mid c$. Залишилося знайти $\beta$. Зауважимо, що останнє рівняння виконується автоматично, адже
\begin{align*}
-\alpha^2 - bd - \alpha c - \alpha \beta^2 a \equiv b(-\beta^2 b - d + \beta c + \beta^3 a) \equiv 0 \pmod {4}
\end{align*}
Отже для знаходження $\beta$ треба скористатися рівнянням $-\beta^3 a + \beta^2 b - \beta c + d \equiv 0 \pmod 4$. Оскільки $p = 2$, у нас існують два можливих розв'язки.

$\beta = 0 \iff 4 \mid d$, а також $\beta = 1 \iff 4 \mid -a+b-c+d$.

\begin{proposition}
Нехай $\Lambda$ -- кубічне кільце при $2 \mid a$. Тоді $2 \mid c$.
\begin{itemize}
\item Припустимо $4 \mid d$, тоді $\Lambda$ допускає розширення елементом $\theta = \dfrac{\omega_2}{2}$.
\item Припустимо $4 \mid -a+b-c+d$, тоді $\Lambda$ допускає розширення елементом $\theta = \dfrac{(-b) \mod 2 + \omega_1 + \omega_2}{2}$.
\end{itemize}
Якщо жодне припущення не виконується, то не існує розширення.
\end{proposition}

\textcolor{red}{Надалі $2 \nmid a$}. Простіше кажучи, $a \equiv 1 \pmod 2$. Маємо систему
\begin{equation*}
\begin{cases}
d - \beta c - \alpha \beta & \equiv 0 \pmod 2 \\
\alpha + \beta b - \beta^2 & \equiv 0 \pmod 2 \\
c +\beta^2 & \equiv 0 \pmod 2 \\
\beta^2 b + d - \beta c - \beta^3 a & \equiv 0 \pmod {4} \\
-\alpha^2 - \beta^2 ac - bd + 2 \beta a d - \alpha c - \alpha \beta^2 a & \equiv 0 \pmod {4}
\end{cases}
\end{equation*}
Аналогічно маємо $\alpha \equiv \beta^2 - \beta b \pmod 2$. Якщо підставити $\alpha$ в перше рівняння, то отримуємо четверте рівняння, тож перше рівняння можна не писати.

Нехай $\beta = 0$ -- розв'язок. Із системи випливає, що $\alpha = 0$, а також $2 \mid c,\ 4 \mid d$. Якщо маємо $2 \mid c,\ 4 \mid d$, то четверте рівняння перепишеться як $\beta^2 b - \beta c - \beta^3 a \equiv 0 \pmod 4$, тому $\beta = 0$ буде тут розв'язком. Також $\beta$ буде розв'язком третього рівняння та останнього (оскільки $\alpha = 0$ із другого).

Нехай $\beta = 1$ -- розв'язок. Із системи випливає, що $\alpha \equiv 1-b \pmod 2$, а також $4 \mid -a+b-c+d$ та $2 \nmid c$. Підставимо $\alpha$ в останнє рівняння:
\begin{align*}
-\alpha^2 - \beta^2 ac - bd + 2\beta a d - \alpha c - \alpha \beta^2 a \equiv \\
\equiv -(1-b)^2 - \beta^2 ac - bd + 2 \beta ad - (1-b) c - (1-b) \beta^2 \equiv \\
ab - ac + 2ad - a - b^2 + bc - bd + 2b - c - 1 \boxed{\equiv}
\end{align*}
Підставимо умову, де $d \equiv a-b+c \pmod 4$, тоді
\begin{align*}
\boxed{\equiv} -2a^2 - 2ab + ac - a + 2b - c -1 = \\
= a - 2a(a-1) + 2b(1-a) - c - 1 + ac \boxed{\equiv}
\end{align*} 
Оскільки $2 \nmid a$, то $2a (a-1) \equiv 0 \pmod 4$ та $2b(1-a) \equiv 0 \pmod 4$
\begin{align*}
\boxed{\equiv} a - c - 1 + ac = (1+c)(a-1) \overset{2\nmid a,\ 2 \nmid c}{\equiv} 0 \pmod 4
\end{align*} 
Навпаки, якщо $2 \nmid c$ та $4 \mid -a+b-c+d$, то, підставивши $\alpha \equiv \beta^2 - \beta b$ в п'яте рівняння, отримуємо, що $\beta = 1$ задовольняє дане рівняння так само, як й третє та четверте.

\begin{proposition}
Нехай $\Lambda$ -- кубічне кільце при $2 \nmid a$.
\begin{itemize}
\item Припустимо $4 \mid d,\ 2 \mid c$, тоді $\Lambda$ допускає розширення елементом $\theta = \dfrac{\omega_2}{2}$.
\item Припустимо $4 \mid -a+b-c+d$ та $2 \nmid c$, тоді $\Lambda$ допускає розширення елементом $\theta = \dfrac{(a-b) \mod 2 + \omega_1 + \omega_2}{2}$. 
\end{itemize}
Якщо жодне припущення не виконується, то не існує розширення.
\end{proposition}

\subsection{$p = 3$} Якщо $3 \mid a$ та $3 \nmid b$, то можемо скористатися результатом \ref{extension_for_p_mid_a_p_nmid_b_when_p_bigger_than_3}. Якщо $3 \mid b$, то (за аналогічними міркуваннями після \ref{extension_for_p_mid_a_p_nmid_b_when_p_bigger_than_3}) маємо $\alpha = 0$ та $3 \mid c$, а $\beta$ шукати можна з рівняння
\begin{align*}
-a \beta^3 + b \beta^2 - c \beta + d \equiv 0 \pmod {9}
\end{align*}
Зазначимо, що $\beta = 0 \iff 9 \mid d$, також $\beta = 1 \iff 9 \mid -a+b-c+d$ та $\beta = 2 \iff 9 \mid -8a+4b-2c+d$.

\begin{proposition}
Нехай $\Lambda$ -- кубічне кільце при $3 \mid a,\ 3 \nmid b$.
\begin{itemize}
\item Припустимо $9 \mid d$, тоді $\Lambda$ допускає розширення елементом $\theta = \dfrac{\omega_2}{2}$.
\item Припустимо $9 \mid -a+b-c+d$, тоді $\Lambda$ допускає розширення елементом $\theta = \dfrac{\omega_1+\omega_2}{2}$.
\item Припустимо $9 \mid -8a+4b-2c+d$, тоді $\Lambda$ допускає розширення елементом $\theta = \dfrac{2\omega_1+\omega_2}{2}$.
\end{itemize}
Якщо жодне припущення не виконується, то не існує розширення.
\end{proposition}

\textcolor{red}{Надалі $3 \nmid a$}. Розв'язуючи систему при $p>3$, ми отримали $\alpha \equiv \beta^2 a - \beta b \pmod p$ а також умови:
\begin{equation*}
\begin{cases}
3 \beta^2 a - 2 \beta b + c & \equiv 0 \pmod p \\
-\beta^3 a + \beta^2 b - \beta c + d & \equiv 0 \pmod {p^2} \\
\end{cases}
\end{equation*}
(абсолютно аналогічна система, як при $p > 3$). Якщо $p = 3$, перше рівняння спроститься до $2\beta b \equiv c \pmod 3$. Рівняння має (єдиний) розв'язок тоді й лише тоді, коли $\gcd(2b,3) \mid -c$.

Припустимо, що $3 \nmid b$. Передусім підставимо $2\beta b$ в рівність з $\alpha$, тоді отримуємо
\begin{align*}
\alpha \equiv c^2 a - 2b^2 c
\end{align*}
Далі друге рівняння системи домножимо на $8b^3$ -- матимемо наступне:
%\begin{equation*}
%\begin{cases}
%-ac^3 - 2b^2 c^2 + 8b^3 d & \equiv 0 \pmod 9 \\
%-a^2 c^4 - ab^2 c^3 + 2b^4 c^2 + 8 ab^3 c d - 8b^5 d & \equiv 0 \pmod 9
%\end{cases}
%\end{equation*}
\begin{align*}
-ac^3 - 2b^2 c^2 + 8b^3 d & \equiv 0 \pmod 9
\end{align*}
\iffalse
В останньому рівнянні можна прибрати вираз $-ab^2 c^3 + 2b^4 c^2 - 8b^5 d$, адже дорівнює нулю за $\pmod 9$, завдяки першому рівнянню.
\begin{equation*}
\begin{cases}
-ac^3 - 2b^2 c^2 + 8b^3 d & \equiv 0 \pmod 9 \\
-a^2 c^4 + 8 ab^3 c d & \equiv 0 \pmod 9
\end{cases}
\end{equation*}
\fi

Припустимо, що $3 \mid b$. Тоді автоматично $3 \mid c$. Аналогічно маємо, що $\beta = 0 \iff 9 \mid d$, також $\beta = 1 \iff 9 \mid -a+b-c+d$ та $\beta = 2 \iff 9 \mid - 8a+4b-2c+d$.

\begin{proposition}
Нехай $\Lambda$ -- кубічне кільце при $3 \nmid a,\ 3 \mid b,\ 3 \mid c$.
\begin{itemize}
\item Припустимо $9 \mid d$, тоді $\Lambda$ допускає розширення елементом $\theta = \dfrac{\omega_2}{2}$.
\item Припустимо $9 \mid -a+b-c+d$, тоді $\Lambda$ допускає розширення елементом $\theta = \dfrac{(a-b) \mod 2 + \omega_1+\omega_2}{2}$.
\item Припустимо $9 \mid -8a+4b-2c+d$, тоді $\Lambda$ допускає розширення елементом $\theta = \dfrac{(4a-2b) \mod 2 + 2\omega_1+\omega_2}{2}$.
\end{itemize}
Якщо жодне припущення не виконується, то не існує розширення.
\end{proposition}


\iffalse
\section{Частинний випадок максимального кубічного кільця}
Нехай $\Lambda$ -- кубічне кільце, яке задається числами $(a,b,c,d)$. Спробуємо розширити кільце елементом $\theta = \dfrac{\alpha + \beta \omega_1}{p}$ при $p = 2$. Оскільки ми хочемо $\tilde{\Lambda} \supsetneq \Lambda$, то перш за все уникнемо випадків, коли $2 \mid \alpha$ та $2 \mid \beta$ водночас (інакше $\theta \in \Lambda$). Для розширення нам необхідно, щоб система \eqref{system_congruent_equations} була розв'язною.

Із точністю до $\pmod 2$, єдина опція залишається -- це обрати $b = 1$. Решта чисел $a,c,d$ із точністю до $\pmod {2^3}$ зобов'язані приймати такі значення:
\begin{center}
\begin{tabular}{c|c|c}
$a$ & $c$ & $d$ \\
\hline
$1$ & $3$ & $3$ \\
$1$ & $7$ & $7$ \\
$3$ & $1$ & $3$ \\
$3$ & $5$ & $7$ \\
$5$ & $3$ & $7$ \\
$5$ & $7$ & $3$ \\
$7$ & $1$ & $7$ \\
$7$ & $5$ & $3$
\end{tabular}
\end{center}
Отримуємо розв'язок $t \equiv -3 \cdot (1)^{-1} = 3 \equiv 5 \pmod 8$. Іншими словами, $\beta \equiv 5 \alpha \pmod 8$. Запишемо всі можливі значення $\alpha,\beta$:
\begin{center}
\begin{tabular}{c|c}
$\alpha$ & $\beta$ \\
\hline
$1$ & $5$ \\
\iffalse $2$ & $2$ \\ \fi
$3$ & $7$ \\
\iffalse $4$ & $4$ \\ \fi
$5$ & $1$ \\
\iffalse $6$ & $6$ \\ \fi
$7$ & $3$ \\
\end{tabular}
\end{center}
Саме при таких параметрах можливе строге розширення $\tilde{\Lambda} \supset \Lambda$. Чи можна буде ще розширити, залежить від значень $\tilde{a},\tilde{b},\tilde{c},\tilde{d}$, які формують кільце $\tilde{\Lambda}$.

Експериментально були отримані наступні дані:
\begin{itemize}
\item Якщо $\alpha = 1,\ \beta = 5$, тобто $\theta = \dfrac{1 + 5 \omega_1}{2}$, то існує розширення елементом $\sigma = \dfrac{\tilde{\alpha} + \tilde{\beta} \tilde{\omega}_1}{5}$.
\iffalse
\begin{tabular}{c|c|c|c|c}
$a$ & $c$ & $d$ & $\alpha$ & $\beta$ \\
\hline
$1$ & $3$ & $3$ & $1$ & $5$ \\
$1$ & $7$ & $7$ & $1$ & $5$ \\
$3$ & $1$ & $3$ & $1$ & $5$ \\
$3$ & $5$ & $7$ & $1$ & $7$ \\
$5$ & $3$ & $7$ & $1$ & $5$ \\
$5$ & $7$ & $3$ & $1$ & $5$ \\
$7$ & $1$ & $7$ & $1$ & $5$ \\
$7$ & $5$ & $3$ & $1$ & $5$ \\
\end{tabular}
\fi
\item Якщо $\alpha = 3,\ \beta = 7$, тобто $\theta = \dfrac{3 + 7 \omega_1}{2}$, то існує розширення елементом $\sigma = \dfrac{\tilde{\alpha} + \tilde{\beta} \tilde{\omega}_1}{7}$.
\item У всіх інших випадках, тобто при $\omega = \dfrac{5 + \omega_1}{2}$ або $\omega = \dfrac{7 + 3 \omega_1}{2}$ розширення не існує (простим $p \neq 3$).
\end{itemize}

Розглянемо тепер $\tilde{\Lambda} \supset \Lambda$, що заданий числами $\tilde{a},\tilde{b},\tilde{c},\tilde{d}$. При певних умов спробуємо дістати ще одне розширення $\tilde{\tilde{\Lambda}} \supset \tilde{\Lambda} \supset \Lambda$.

Експериметально було встановлено, що при $b \in \{1,3,5,7\}$ та всіх можливих $a,c,d$ (які задані до $\pmod 8$), які утворюють кільця, а також задовольняють умову системи, ми маємо розширення $\Lambda \supset \tilde{\Lambda}$. Подальше розширення елементом $\tilde{\tilde{\theta}} = \dfrac{\tilde{\alpha} + \tilde{\beta} \tilde{\omega}_1}{q}$, де $q = 2$ або $q > 3$ -- просте, не допускається. Залишається перевірити, чи можливе розширення при простому $q = 3$.

Виявляється, що при $q = 3$ теж нема можливостей для розширення.

Зауважимо, що при $\alpha = 1, \beta = 1$ маємо $\tr [\theta] = \dfrac{1}{2} (3+b)$, звідси $2 \tr [\theta] = 3 + b$, або $\tilde{b} = -3 - b$.

Отже, для розширення треба, щоб $3 \mid \tilde{b}$, тобто треба $3 \mid b$.
\fi


\iffalse
Розпишемо матриці $[\omega_1],\ [\omega_2]$ оператора множення. Для множення на $\omega_1$ маємо
\begin{align*}
\omega_1 \cdot 1 = \omega_1 = 0 \cdot 1 + 1 \cdot \omega_1 + 0 \cdot \omega_2 \\
\omega_1 \cdot \omega_1 = -ac + b \omega_1 + a \omega_2 \\
\omega_1 \cdot \omega_2 = ad = ad \cdot 1 + 0 \cdot \omega_1 + 0 \cdot \omega_2
\end{align*}
Водночас для множення на $\omega_2$ маємо
\begin{align*}
\omega_2 \cdot 1 = \omega_2 = 0 \cdot 1 + 0 \cdot \omega_1 + 1 \cdot \omega_2 \\
\omega_2 \cdot \omega_1 = ad = ad \cdot 1 + 0 \cdot \omega_1 + 0 \cdot \omega_2 \\
\omega_2 \cdot \omega_2 = -bd + d \omega_1 + c 
\end{align*}
Звідси отримуємо матриці
\begin{align*}
[\omega_1] = \begin{pmatrix}
0 & -ac & ad \\
1 & b & 0 \\
0 & a & 0
\end{pmatrix},\quad [\omega_2] = \begin{pmatrix}
0 & ad & -bd \\
0 & 0 & d \\
1 & 0 & c
\end{pmatrix}
\end{align*}
\fi

\newpage

\chapter{Кубічні кільця та цілочисельні зображення}\label{chaper3}
Розпочати необхідно з такого результату.

\begin{proposition}
Нехай кубічне кільце $\Lambda$ має розширення $\tilde{\Lambda}$, причому $[\tilde{\Lambda}: \Lambda] = p$. Тоді $\Delta_\Lambda = p^2 \Delta_{\tilde{\Lambda}}$. Іншими словами, $p^2 \mid \Delta_\Lambda$.
\end{proposition}

На жаль, у зворотний бік дане твердження не виконується. Можна розглянути кубічне кільце $\Lambda$, яке задається числами $(6,6,1,0)$. Його дискримінант $\Delta_\Lambda = 12$, причому $2^2 \mid 12$. Утім оскільки $2 \mid a$, то обов'язково $2 \mid c$, проте не в даному випадку, тому ніякого розширення нема.

Також вимушений зауважити, чому розглядаються два випадки розширень окремо. Можна взяти кільце $\Lambda$, яке задається числами $(2,2,0,2)$, у нього дискримінант $\Delta_\Lambda = -496$. Хоча $4 \mid -496$, але дане кільце не допускає розширення простим числом $p=2$ шляхом заміни одного з двох базисних елементів. Проте, замінюючи два базисні одночасно, ми досягнемо розширення при $p=2$.

\section{Про зображення кубічних кілець}
Нехай $\Lambda$ -- кубічне кільце та $M \subset \Lambda$ -- максимальне надкільце. Факторгрупа $M/_\Lambda$ буде абелевою, тому за основною теоремою про абелеві групи запишемо його в такому вигляді:
\begin{align*}
M/_\Lambda = \mathbb{Z}_{n_1(\Lambda)} \oplus \mathbb{Z}_{n_2(\Lambda)},
\end{align*}
де числа $n_1(\Lambda) \mid n_2(\Lambda)$.

\begin{theorem}
\label{integral_representation_theorem}
Кубічне кільце $\Lambda$ має скінченне число неізоморфних нерозкладених зображень $\iff n_1(\Lambda)$ -- вільне від квадратів.
\end{theorem}

\section{Кільця, що допускають лише розширення простим числом $p = 2$}
I. Розглянемо кільце $\Lambda$, яке задається числами $(a,a,2^k-a,2^k-a)$, де число $k \geq 2$ та $a \neq 0$. Його дискримінант $\Delta_\Lambda = 2^{2k+2}a(a-2^k)$. Отже, можливо, щонайбільше $k+1$ розширень простим числом $p = 2$.

\begin{lemma}
Кільце $\Lambda = \left<1,\omega_1,\omega_2\right>$ допускає розширення до кубічного кільця $\tilde{\Lambda} = \left< 1,\omega_1-a,\dfrac{\omega_1+\omega_2}{2} \right>$.
\end{lemma}

\begin{proof}
Позначимо $(a,b,c,d) = (a,a,2^k-a,2^k-a)$. Розіб'ємо доведення на два випадки:
\begin{itemize}
\item Нехай $4 \nmid a$. Отже, $2 \nmid a$. Зауважимо, що $4 \mid -a + a - (2^k-a) + (2^k-a) = 0$. Значить, допускається розширення елементом $\theta = \dfrac{(a-a) \mod 2 + \omega_1 + \omega_2}{2} = \dfrac{\omega_1+\omega_2}{2}$. 
\item Нехай $4 \mid a$ (нагадуємо, що $a \neq 0$), тому $4 \mid 2^k-a$. Значить, допускається розширення елементом $\theta = \dfrac{(-a) \mod 2 + \omega_1 + \omega_2}{2} = \dfrac{\omega_1 + \omega_2}{2}$ (насправді, ми можемо розширитися елементом $\theta =\dfrac{\omega_2}{2}$, утім це нас не зовсім цікавитиме).
\end{itemize}
Проведемо розширення до кільця $\tilde{\Lambda} = \Lambda[\theta]$. Тоді після нормування базису отримуємо $\tilde{\Lambda} = \left< 1,\omega_1-a, \dfrac{\omega_1+\omega_2}{2}\right>$. При цьому, оновлюючи значення $a,b,c,d$, отримуємо, що кільце $\tilde{\Lambda}$ числами $(2a,-2a,2^{k-1},0)$.
\end{proof}

\begin{lemma}
Нехай $\Lambda = \left<1,\omega_1,\omega_2\right>$ -- кубічне кільце, що задається числами $(2a,-2a,2^{k-1},0)$. Припустимо, що $4 \nmid a$. Тоді кільце $\Lambda$ допускає розширення до кільця $M$.
\end{lemma}

\begin{proof}
Позначимо $(a,b,c,d) = (2a,-2a,2^{k-1},0)$. Зауважимо, що $2 \mid c$ та $4 \mid d$. Отже, можна розширитися елементом $\theta = \dfrac{\omega_2}{2}$.

Маємо кільце $\tilde{\Lambda} = \left<1,\omega_1, \dfrac{\omega_2}{2}\right>$. Його оновлені значення $(a,b,c,d) = (2^2a,-2a,2^{k-2},0)$. Аналогічно $2 \mid c,\ 4 \mid d$, тому знову можемо розширюватися елементом $\theta = \dfrac{\tilde{\omega_2}}{2} = \dfrac{\dfrac{\omega_2}{2}}{2} = \dfrac{\omega_2}{2^2}$.

Маємо кільце $\tilde{\Lambda} = \left<1, \omega_1, \dfrac{\omega_2}{2^2}\right>$. Його оновлені значення $(a,b,c,d) = (2^3a,-2a,2^{k-3},0)$. І так далі...

У результаті отримуємо кільце $M = \left< 1, \omega_1, \dfrac{\omega_2}{2^k} \right> \supset \Lambda$, де його значення $(a,b,c,d) = (2^k a, -2a, 1,0)$.
\end{proof}

Отже, маючи кубічне кільце $\Lambda = \left<1,\omega_1,\omega_2\right>$ зі значеннями $(a,a,2^k-a,2^k-a)$ при $4 \nmid a$, ми можемо отримати розширення $M = \left<1, \omega_1 - a, \dfrac{\omega_1+\omega_2}{2^{k}}\right>$, завдяки двом лемам. Його дискримінант $\Delta_M = 4a(a-2^k)$. Кільце $M$ задається числами $(2^ka,-2a,1,0) = (\tilde{a},\tilde{b},\tilde{c},\tilde{d})$.

Зауважимо, що $2 \mid 2^k a$, утім $2 \nmid 1$, тому подальшого розширення простим числом $p = 2$ нема заміною третього базисного елементу. Спробуємо замінити другий базисний елемент. Запишемо кільце $M = \left<1, \dfrac{\omega_1+\omega_2}{2^k}, \omega_1-a \right>$, тоді дане кільце задається числами $(0,1,-2a,2^ka)$. Цього разу $2 \mid 0,\ 2 \mid -2a,\ 4 \mid 2^k a$. Отже, допускається розширення до $\tilde{M} = \left< 1, \dfrac{\omega_1+\omega_2}{2^k}, \dfrac{\omega_1-a}{2}\right>$, яке задається числами $(0,1,-a,2^{k-2}a)$. Оскільки $\Delta_{\tilde{M}} = a(a-2^k)$ та $4 \nmid a$, то $4 \nmid \Delta_{\tilde{M}}$, тож інших розширень нема при $p = 2$. Змінимо останні два базисні елементи місцями назад.

У результаті $\left<1,\omega_1,\omega_2\right> = \Lambda \subset \tilde{M} = \left< 1, \dfrac{\omega_1-a}{2}, \dfrac{\omega_1+\omega_2}{2^k}\right>$, причому $\tilde{M} \leftrightarrow (2^{k-2}a,-a,1,0)$.

\begin{proposition}
Кільце $\tilde{M}$ -- максимальне при $a \not\equiv 2^k \pmod {p^2}$.
\end{proposition}

\begin{proof}
Через його дискримінант уже зазначали, що кільце $\tilde{M}$ не допускає розширення простим числом $p = 2$. Припустимо, що $\tilde{M}$ можна розширити простим числом $p > 3$. Розіб'ємно на кілька можливих випадків:
\begin{itemize}
\item $p \mid \tilde{a}$, тобто $p \mid 2^{k-2}a$. Оскільки $p>3$, то звідси маємо $p \mid a$. Отже $p \mid \tilde{b}$, але тоді обов'язково $p \mid \tilde{c}$, тобто $p \mid 1$, що неможливо.

\item $p \nmid \tilde{a}$, тобто $p \nmid 2^{k-2} a$. Оскільки $p>3$, то звідси $p \nmid a$. Отже, $p \nmid \tilde{b}$. Значить, необхідно розглянути систему рівнянь \eqref{system_of_equations_when_a_nmid_p_and_b_nmid_p}. Підставивши наші $\tilde{a},\tilde{b},\tilde{c},\tilde{d}$, матимемо такий вигляд:
\begin{equation*}
\begin{cases}
3 \cdot 2^{k-2} a \beta^2  + 2a \beta + 1 & \equiv 0 \pmod p \\
- 2^{k-2} a \beta^3 - a\beta^2 - \beta & \equiv 0 \pmod {p^2} \\
\end{cases}
\end{equation*}
Зауважимо, що $\beta \neq 0$, інакше отримали б $1 \equiv 0 \pmod p$ із першого рівняння. Тому можемо скоротити на $\beta$ друге рівняння. Якщо спустити всі рівняння до $\pmod p$ та додати, то отримуємо $2^{k-1} \beta \equiv -1 \pmod p$ (розв'язок існує), внаслідок чого $\alpha \equiv -1 \pmod p$. Підставивши в друге рівняння, отримуємо обмеження $a \equiv 2^{k} \pmod {p^2}$.
\end{itemize}
Отже, більше нема розширень простим числом $p > 3$. Аналогічним чином прийдемо то неможливості розширюватися числом $p = 3$.
\end{proof}

Отже, кубічне кільце $\Lambda \leftrightarrow (a,a,2^k-a,2^k-a)$ при $k \geq 2$ та $4 \nmid a$ та $a \not\equiv 0 \pmod {p^2}$ (де $p$ -- просте) розширюється до максимального кільця $\tilde{M} \leftrightarrow (2^{k-2}a, -a,1,0)$, причому $[M:\Lambda]^2 = \dfrac{\Delta_\Lambda}{\Delta_M} = 2^{2k+2}$. Тобто $[M:\Lambda] = 2^{k+1}$.

Розглянемо факторгрупу $M/_\Lambda$. Пригадаємо, що коли маємо вкладення $\mathbb{Z}^3 \hookrightarrow \mathbb{Z}^3$, то їхні базисні елементи мають бути пропорційними. Зокрема базис $\left\{ 1, \dfrac{\omega_1 - a}{2}, \dfrac{\omega_1+\omega_2}{2^k} \right\}$ кільця $M$ пропорційний базису $\{1,\omega_1-a,\omega_1+\omega_2\}$ кільця $\Lambda$. Відображення $\Lambda \hookrightarrow M$ задається як $(x,y,z) \mapsto (x,2y, 2^k z)$. Отже, $\Lambda \cong \mathbb{Z} \oplus \mathbb{Z} \oplus 2^k \mathbb{Z}$, а також $M \cong \mathbb{Z} \oplus 2\mathbb{Z} \oplus \mathbb{Z}$. Значить, факторгрупа $M/_\Lambda \cong \mathbb{Z}/_{2\mathbb{Z}} \oplus \mathbb{Z}/_{2^k\mathbb{Z}} = \mathbb{Z}_2 \oplus \mathbb{Z}_{2^k}$.

Отже, досліджене кіьце задовольняє теорему \ref{integral_representation_theorem}.
\bigskip \\

II. Розглянемо кільце $\Lambda$, яке задається числами $(0,b,2^k,0)$ при $b \neq 0$. Його дискримінант $\Delta_\Lambda = 2^{2k} b^2$. Отже, можливо, щонайбільше $k$ розширень простим числом $p = 2$.

\begin{proposition}
Кільце $\Lambda$ допускає розширення до кільця $M = \left< 1,\omega_1,\dfrac{\omega_2}{2^k}\right>$. Причому дане кільце -- максимальне.
\end{proposition}

\begin{proof}
Позначимо $(a,b,c,d) = (0,b,2^k,0)$. Оскільки $2 \mid a$ та $2 \mid c$ та $4 \mid d$, то автоматично допускається розширення до кільця $\tilde{\Lambda}$ елементом $\theta = \dfrac{\omega_2}{2}$. Базисом буде $\tilde{\Lambda} = \left<1,\omega_1,\dfrac{\omega_2}{2}\right>$, а оновленими значеннями $(a,b,c,d) = (0,b,2^{k-1},0)$.

Маючи кільце $\tilde{\Lambda}$, повторюємо дану процедуру абсолютно аналогічним чином, допоки можливе ділення $c$ на $2$.

У результаті виникне кільце $M = \left<1,\omega_1,\dfrac{\omega_2}{2^k}\right>$, яке задається числами $(a,b,c,d) = (0,b,1,0)$. Доведемо, що дане кільце -- максимальне.

Припустимо, що $M$ допускає розширення простим числом $p > 3$. Із системи рівнянь випливає, що $2\alpha \equiv -1 \pmod p$. Підставивши в останнє рівняння $2\alpha$, а також значення $a,b,c,d$, отримуємо $-\alpha^2-\alpha \equiv 0 \pmod {p^2} \implies -4 \alpha^2 - 2 \cdot 2\alpha \equiv 1 \equiv 0 \pmod p$, що неможливо (аналогічно при $p=3$). Також неможливо розширитися простим числом $p =2$, оскільки $4 \nmid \Delta_M$.
\end{proof}

Аналогічними міркуваннями доведемо, що $\Lambda/_M \cong \mathbb{Z}/_{2^k \mathbb{Z}}$. Отже, досліджене кільце $\Lambda$ задовольнує теорему \ref{integral_representation_theorem}. 

\section{Кільце, що допускає різні максимальні кільця}
Розглянемо кільце $\Lambda \leftrightarrow (4,4,4,4)$, його дискримінант $\Delta_\Lambda = -4096$. Розглянемо наступну діаграму.
\begin{figure}
\centering
\begin{tikzcd}
& \underset{(8,4,2,1)}{\tilde{\Lambda}} \arrow[hookrightarrow, two heads, r, "\frac{\omega_2}{2}"] & \underset{(1,-2,2,0)}{\tilde{\tilde{\Lambda}}} \arrow[hookrightarrow, r, "\frac{\omega_2}{2}"] & \underset{(2,-2,1,0)}{\tilde{\tilde{\Lambda}}} \\
& & & \\
\underset{(4,4,4,4)}{\Lambda} \arrow[hookrightarrow,ruu,"\frac{\omega_2}{2}"] \arrow[hookrightarrow,rdd,"\frac{\omega_1+\omega_2}{2}"] & & & \\
& & \underset{(16,-8,2,0)}{\tilde{\tilde{\Lambda}}} \arrow[hookrightarrow,r,"\frac{\omega_2}{2}"] & \underset{(32,-8,1,0)}{\tilde{\tilde{\tilde{\Lambda}}}} \\
& \underset{(8,-8,4,0)}{\tilde{\Lambda}} \arrow[hookrightarrow,ru,"\frac{\omega_2}{2}"] \arrow[hookrightarrow,rd,"\frac{\omega_1+\omega_2}{2}"]  & & \\
& & \underset{(16,-32,22,-5)}{\tilde{\tilde{\Lambda}}} &
\end{tikzcd}
\end{figure}


\newpage

\phantomsection
\chapter*{Висновки}
\addcontentsline{toc}{chapter}{Висновки}

У роботі ??????

\newpage

\phantomsection
\renewcommand{\bibname}{Список використаних джерел}

\begin{thebibliography}{99}
\addcontentsline{toc}{chapter}{Список використаних джерел}
\itemsep=0pt

\bibitem{boyko-thesis}
Бойко В.М.,
Узагальненi оператори Казiмiра,
сингулярнi модулi редукцiї
та симетрiї диференцiальних рiвнянь,
Дис. \dots\ док. фіз.-мат. наук,  Інституту математики НАН України, Київ, 2018, 338~с., \url{https://www.imath.kiev.ua/~boyko/BoykoThesis.pdf}.



\bibitem{boyko2021}
Boyko V.M., Lokaziuk O.V., Popovych R.O.,
Admissible transformations and Lie symmetries of linear systems of second-order ordinary differential equations, \href{https://arxiv.org/abs/2105.05139}{arXiv:2105.05139}.

\bibitem{Maple17}
Maple 17, \url{https://www.maplesoft.com/products/Maple/}.


\bibitem{Olver1995}
Olver P.J., Equivalence, invariants, and symmetry, Cambridge, University Press Cambridge, 1995, xvi+525~pp.,
\url{https://doi.org/10.1017/CBO9780511609565}.


\bibitem{PopovychBoykoNesterenkoLutfullin2003}
Popovych R.O., Boyko V.M., Nesterenko M.O., Lutfullin M.V., Realizations of real low-dimensional Lie algebras, \textit{J.~Phys.~A} \textbf{36} (2003), no.~26,
7337--7360, \url{https://doi.org/10.1088/0305-4470/36/26/309}; \href{https://arxiv.org/abs/math-ph/0301029}{math-ph/0301029}.



\end{thebibliography}

\appendix

\chapter{Назва додатку}\label{appendix1}

\section{Назва секції додатку}\label{appendix1.1}

\end{document}

\newpage

\bibliographystyle{plain} %plain %sigma %amsalpha %ugost2008
\bibliography{ref}



